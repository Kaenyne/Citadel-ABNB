# L3 ADR and hotel audit — preregistration

2026-09-13; owner `adr_hotel`; branch `codex/lane3-full`; claim in `WORKBOARD_L3_v1.md`. Saved before executing the audit. All writes are confined to new L3 package/output files. This audit reuses frozen research; it does not claim the original rule was selected without seeing earlier results.

## Locked methods and pass lines

1. Import only the side-effect-free ADRv3 `S1_scoring.py`. Recompute the plain last-quarter residual and imposed K paths from components, then independently score their stored paths. Match all 16 model/target/window/FX rows of P1 on RMSE, naive ratio and jackknife limits within 1e-9, with exact n. Match J3's 32 reproduction rows within its 0.001 tolerance. Retain ADR windows 2024Q1–2026Q2 (10) and 2024Q2–2026Q2 (9), explicitly distinct from the ABNB harness W1/W2.
2. Preserve the original research hurdle: on dollar ADR growth, ratios and every drop-one ratio below one for both euro and basket estimators in both ADR windows. Report the original rounded-ex-FX target alongside. Additionally score continuous predictions against reported ex-FX +/-0.5 intervals as a separate descriptive sensitivity; never relabel it as the frozen ADRv3 criterion or a main-harness result.
3. Reconcile historical residual = ex-FX minus geo/size/LOS/new-business/interaction to 1e-9. Trace realised current-quarter mix, missing/fill terms, FX estimation vintages, post-hoc rule selection, K calibration, and the 2023 half-point entries. No PIT claim without input-vintage proof. Preserve regional/new-listing failed additions and do not export their fitted coefficients as usable forecast inputs.
4. Rebuild each frozen card's ex-FX sum, additive FX bridge, ADR dollars, nights*ADR GBV, and same-quarter take-rate revenue comparison within the relevant final rounding precision (0.005 pp, $0.005 ADR, $0.005bn GBV, $0.5m revenue, plus 1e-9 arithmetic tolerance). K must equal 0.007 times changes in the assumed year-over-year migrated nights share; Q4 is cumulative from Q2. Reproduce wider K bounds using 0.038. Bands are scenario constructions, not confidence intervals. Never export the contemporaneous revenue comparison as a forecast or lagged revenue kernel.
5. Hotel comparators: rebuild the committed monthly US lodging CPI and BEA hotel-price index growth from their raw public series, match the existing monitor within 0.051 pp. Do not collect new observations. Aggregate only complete three-month quarters; report incomplete-quarter coverage separately, missing months explicitly, and any stored rounding differences. On ADR windows above, plus the available 2023Q1–2026Q2 descriptive history, report Pearson and Spearman associations with Airbnb ex-FX and reported ADR, paired n, drop-one Pearson range, and change-in-growth association. This is a robustness/coverage diagnostic, with no predictive promotion or causal pass line.
6. Recompute the existing 13-market hotel coverage audit: 13 market rows; count available production outcomes, existing page-capture coverage and registered capacity evidence without pooling heterogeneous rooms/licenses or setting missing bookings to zero. Actual Airbnb hotel pricing, occupancy and incremental demand remain unidentified without outcomes.
7. Produce L4 rows with quarter, metric, scenario, value/lower/upper, units, information_date, evidence_status, source_reference, treatment, baseline_replaced, embedded_fx and limitations. ADR totals replace ADR totals; component rows are descriptive with parent scenario linkage; embedded FX is never added twice. No team/card/nights adoption or registration. Hash inputs before/after and fail on changes. Repeat runs must be byte-identical. Unit tests cover scoring alignment/missing values, zero benchmark variance, interval boundaries, complete-quarter aggregation, leakage rejection and adapter duplication/double-counting metadata.

## Status definitions

Implementation PASS requires all integrity/reproduction checks. Research PASS/FAIL applies only to the original descriptive ADR criterion. PIT qualification, causal fees, hotel-to-Airbnb pricing transmission and investment adoption are separate and may remain partial/unidentified. Publish failed checks; do not tune methods to improve them.

## RESUME

Run the new package after implementation, preserve original files and write `L3_ADR_HOTEL_RESULTS.md` with exact commands, counts, failures, interpretation and audit findings. L4 may consume the versioned scenarios with qualifications; it owns any forecast or registration.
