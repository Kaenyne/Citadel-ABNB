# L3 integration addendum — conversion priority and bundle review

2026-09-13. Extends the original integration preregistration after the user's explicit conversion-validation priority. All previous research and pass lines remain intact.

The bundle now requires five packages: cohort FX, fee panel, ADR/hotel, NCLH and GBV-to-revenue conversion validation. The conversion package preregisters its own model, 22-quarter fit, chronological tests, uncertainty and sensitivity protocol. A new analytical fit is never silently substituted for the operational fixed-weight kernel. The lead independently checks the implementation, matched dates/n and presentation labels before including its accepted analytical specification and claim ledger.

Independent bundle review found a checksum bug: verification ignored nested files called SHA256SUMS.json. Verification now excludes only the root manifest and rejects unexpected nested files. It also requires selected source packages and inputs to be committed/clean, and replacement rows to name their embedded-FX treatment. Initial findings remain in L3_BUNDLE_INDEPENDENT_REVIEW_v1.md. After regressions, the schema/checksum suite has 13 passing tests. Historical source periods/timestamps remain in source_period/source_information_date; the bundle's information_date is the actual September-13 L3 analysis date, not the date of the latest underlying quarterly print.

The lead will build a new immutable bundle only after the final reviewed code/input commit, preserve exact payload bytes through bundle-local Git attributes, and supply the separate bundle-containing commit to L4. No workbook, valuation, memo, trade decision or ABNB registry work is included.

## RESUME

Wait for independent package closures and conversion validation, run the final integrated reproduction into new directories, check frozen hashes and build the committed handoff. Verify the Git-stored payload against its checksum manifest before publication, then leave merge/adoption to the user/team.
