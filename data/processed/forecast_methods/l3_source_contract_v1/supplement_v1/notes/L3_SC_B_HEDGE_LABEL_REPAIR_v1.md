# SC-B — unresolved hedge basis label correction

cohort_fx · 2026-09-14 · before final independent closure. Earlier results_v1/v2 are preserved. New canonical output will be results_v3.

The conditional-use gate's phrase `hold_embedded_unchanged_no_new_hedge` is stronger than the evidence supports. The aggregate inherited-lambda arithmetic does not identify its hedge component H. Omitting a new hedge overlay cannot certify that a multiplier preserves H dollars. The repaired phrase is `inherited_hedges_unresolved_no_new_hedge_or_afterhedge_claim`; the row hedge basis explicitly disclaims certified hedge preservation.

For a synthetic illustration only, B=100, H=10 and k=1.05 give B*k=105, while a properly reconciled operating-only application gives (B-H)*k+H=104.5. The discrepancy is H*(k-1). The scenario adapter's aggregate ratio cannot silently be relabeled a certified operating ratio. Missing H stays unavailable. This is a clarification of the conditional exhibit's scope, not a new ABNB forecast, hedge estimate or accounting discovery.

All original 1,080 source rows/values remain unchanged. New contract metadata, README and gate tests will reject the prior overstrong phrase and accept only the explicit unresolved convention. The three aggregate arithmetic alternatives remain mutually exclusive conditional exhibits. Direct L4 application remains blocked. Tests will include the nonzero-H counterexample and rejection of a certified-unchanged-hedge claim before a new output is frozen.

## RESUME

SC-A should review the final results_v3 contract and retain this correction alongside the earlier receipt. Lead must preserve the distinction between an unresolved aggregate scenario and an after-hedge model calculation requiring a certified baseline split and compatible ratio.
