The first supplemental reviewer replay correctly stopped because its independently written training `y` omitted subtraction of latest company GBV growth. The author training `y` was correct. The reviewer formula was copied into `supplement_checks_v2.py` with this one correction (`actual_yoy - latest_company_yoy`) and rerun into new `supplement_v2/`. No author source, result, model or preregistration changed. The first checker and this failed attempt remain available.
