The first independent Street checker exited at common_model_metrics row4 because the checker expected the comparator label `street`, while the saved author table correctly labels it `DoltHub`. This was a reviewer schema mismatch, not a failed numerical or source check. The original checker and output directory are preserved. `street_checks_v2.py` changes only that comparator-label literal and writes to a new `street_v2/` output directory. No author model, source or result was changed.
