# Protocol source clarification before freeze

2026-09-14 · worker B · no performance evaluation. This new note resolves two pending points in `PROTOCOL_PROPOSAL_v1.md`; parent must incorporate these into the separately frozen final protocol.

Source auditor A reports that all historical guide endpoints in the immutable ledger are multiples of USD10m and commonly quoted in two-decimal USD billions. These are deliberately issued management guide endpoints, not noisy measured financial quantities. Therefore the observed target is the exact arithmetic midpoint of the issued range. Do **not** infer a +/-USD5m measurement-error band merely from formatting.

For the parent's requested interval-fair primary comparison, lock midpoint +/-USD0.5m as an **administrative project integer-USDm scoring convention**, explicitly not empirically verified midpoint measurement uncertainty. Publish ordinary raw midpoint RMSE/MAE/bias with the interval-fair counterparts. Keep the protocol's proposed both-window and paired-year-deletion hurdle on interval-fair errors as requested; state whether raw midpoint results agree. No source rounding assumption is selected after performance. Use the same fixed convention for all three forecast methods and all targets. In other contexts, letter-rounded measured integers still retain their source-unit +/-0.5 intervals.

A has not identified any complete compatible pre-origin archived GBV forecast series in the immutable baseline. Uniform latest-observed GBV YoY persistence is therefore the one reconstructed upstream rule for every forecast requiring an unprinted lag. Later/at-print L0 estimates are unavailable at these origins and are not substituted. This is an availability finding, not an outcome comparison.

## RESUME

Parent incorporates these two resolutions into the final protocol and hash-freezes it before worker A evaluates new historical outcomes. No reopening of the rule or interval width is needed after evaluation begins.
