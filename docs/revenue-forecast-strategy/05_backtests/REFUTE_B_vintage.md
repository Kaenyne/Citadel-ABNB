# REFUTE_B_vintage — independent FY-guide eligibility audit

Agent `/root/ref_x_vintage` executing B vintage · 12 September 2026 task date · parent owns board, branch, scorer and git · token/session totals unavailable unless recorded below.

## Verdict

**survived** on the complete original sentence. The source ledger contains **zero numerical FY revenue dollar guides**; the supplied register contains **zero attributed FY revenue consensus observations strictly before any scored guide**. Either missing comparator is sufficient to prevent the corresponding revision test, irrespective of how well a kernel could forecast. Independent origin selection reproduces **0/14 W1 and 0/10 W2**. Current vendor-stamped comparisons remain arithmetic scenarios with no validated revision alpha. This verdict concerns the supplied data and stated test, not every possible historical dataset or differently timed study.

## Pre-registered audit

Original exact sentence: “At strict pre-guide dates, the supplied data yield zero eligible FY-guide-revision tests in W1 (0/14) and W2 (0/10), so the live kernel–consensus gap is a conditional scenario, not validated revision alpha.”

Written before independent calculations. No statistical pass line applies to this refuter. Test the full original sentence through at least five explicit attacks, returning survived / refuted / partial. Count guide origins and FY dollar-guide/revision candidates directly from source inputs; audit every relevant vendor/date in the supplied consensus register; attempt a reference-date or same-day splice; check return-field and registry availability. Do not read B/K0 code or B derived outputs. Do not turn missing pairs into zero errors or import live September estimates into historical origins. Write only this note.

## Results: seven explicit refutation attempts

1. **Claim: there are 14 W1 and 10 W2 origins, all ineligible. Attack: reconstruct the calendars and look for an omitted eligible quarter or an August LIVE event included in the denominator. Result: survived.** From the named frozen calendar, `next_quarter_guided` selects targets 2023Q1–2026Q2, giving **14** origins from **2023-02-14 through 2026-05-07**. Restricting to 2024Q1 onward gives **10**, starting **2024-02-13**. The 2026-08-06 origin guides 2026Q3 and is outside both windows. Every selected origin has zero strictly earlier same-year numerical FY-dollar-guide denominators and zero strictly earlier attributed FY revenue consensus values. These are origin counts, not 14/10 successful forecasts or independent confirmations.

2. **Claim: the FY-guide revision test has no eligible outcomes. Attack: use the FY ledger's three numerical-looking revenue entries as usable guides. Result: survived.** The ledger has **47 FY rows** across metrics, but **zero `revenue_usd_m` FY rows**. Its only three revenue rows have `metric=revenue_yoy_pct`, `guide_type=bucket`, and percentage units. They encode FY2026 language at **11% on 2026-02-12, 14% on 2026-05-07, and 15% on 2026-08-06**. The first and third are explicitly floor language; the second is a broad low-to-mid-teens range. Their centers are not company-issued dollar midpoints. Treating the centers as exact data creates two artificial numerical changes, +3pp and +1pp, but does not supply the pre-registered dollar denominator or threshold. Treating each center as a letter-rounded ±0.5 interval would also tighten the supplied 10–12 / 12–16 / 14–16 encodings and improperly close the floor language. Only the final scored origin has an earlier FY growth bucket; it still has no numerical dollar guide. The three raw HTML `source_file` paths are missing locally, so the language assessment here uses the supplied ledger's quote/type/note fields, not independently verified letter text. The zero count is a fact about that supplied ledger.

3. **Claim: no strictly earlier attributed FY consensus supports the historical test. Attack: check every FY revenue register row and try a September splice. Result: survived.** There are **12 FY revenue rows**, all attributed and PIT-usable under their own dates, covering only FY2026/FY2027. All timestamps fall **2026-09-03 through 2026-09-11**, later than the last scored guide, 2026-05-07. No FY2023/FY2024/FY2025 revenue panel exists in this input. Ignoring timestamps would falsely make same-year consensus appear available at **2/14 W1 and 2/10 W2** origins (the two FY2026 origins), but cannot make those September observations historical. Matching period and vendor while enforcing dates restores zero. All 30/60/90-day revision pair counts are zero because there is no valid starting observation; the result does not depend on a particular choice of future revision endpoint.

| Exact vendor in FY revenue register | Timestamp | FY2026, USD M | FY2027, USD M | n source rows | Historical origin eligibility |
|---|---|---:|---:|---:|---|
| S&P Global Market Intelligence | 2026-09-03 | 14,160 | 15,760 | 2 | None |
| Zacks | 2026-09-04 | 14,100 | 15,730 | 2 | None |
| S&P Global Market Intelligence via StockAnalysis | 2026-09-10 | 14,160 | 15,770 | 2 | None |
| Alpha Vantage (aggregated sell-side panel) | 2026-09-11 | 14,155 | 15,758 | 2 | None |
| Yahoo Finance | 2026-09-11 | 14,160 | 15,790 | 2 | None |
| Zacks | 2026-09-11 | 14,100 | 15,740 | 2 | None |

4. **Claim: strict pre-guide timing is materially different from at/after-guide timing. Attack: admit the same-day FY guide or the just-released GBV and call the result a pre-guide forecast. Result: survived.** At 2026-02-12, the first FY2026 growth statement is same-day and inadmissible as an input under the stated strict rule. At 2026-05-07, only February's growth bucket is earlier; May's updated language is same-day. Even allowing these same-day statements would produce no numerical FY-dollar midpoint. More decisively, grant an adversary perfect four-quarter kernel forecasts at all 14 origins: the missing dollar guide still forces zero eligible guide-revision pairs, and the missing historical FY consensus still prevents the consensus-revision test. This establishes the headline count without relying on B's implementation or its separate claim about K0's missing historical RNPL nowcasts. I did not independently reconstruct K0's historical abstentions or complete FY forecasts; they are unnecessary to this sufficient exclusion proof. A post-letter or qualitative-guide study would be a separately specified test.

5. **Claim: the live kernel–consensus gap is conditional, not historically validated alpha. Attack: verify that the live comparators are genuinely earlier than 12 September and see whether their positive gaps could themselves validate revision prediction. Result: survived.** The note's chosen current Q4/FY2026 vendor labels, values and dates match the register. Latest-per-exact-label observations are **1–9 days old** on 2026-09-12; none is misdated as historical. The register has no 2027Q1 revenue consensus. Subtracting the note's **reported** $14,309.14M conditional FY2026 point gives the arithmetic below. This checks denominator selection and arithmetic, not an independently reproduced kernel point or observed subsequent revisions. Repeated vendor rows and the two S&P labels do not become independent validation events. The $3,158M Q4 comparator remains labelled Alpha Vantage, exactly as supplied; I do not relabel its source vendor.

| FY2026 latest exact-label comparator | Timestamp | Consensus, USD M | Gap using the note's reported $14,309.14M point | n scenario comparison |
|---|---|---:|---:|---:|
| S&P Global Market Intelligence | 2026-09-03 | 14,160 | +1.053249% | 1 |
| S&P Global Market Intelligence via StockAnalysis | 2026-09-10 | 14,160 | +1.053249% | 1 |
| Alpha Vantage (aggregated sell-side panel) | 2026-09-11 | 14,155 | +1.088944% | 1 |
| Yahoo Finance | 2026-09-11 | 14,160 | +1.053249% | 1 |
| Zacks | 2026-09-11 | 14,100 | +1.483262% | 1 |

Using the note's displayed Q4 point $3,214.78M produces +1.797973% against Alpha Vantage $3,158M (2026-09-11), +1.733544% against S&P via StockAnalysis $3,160M (2026-09-10) and Yahoo $3,160M (2026-09-11), and +0.461875% against Zacks $3,200M (2026-09-11), n=1 each. The note displays +1.733% on the $3,160 comparisons; its undisplayed point precision could explain that last-decimal difference because $3,214.78M is already rounded to cents of a million. No material data or vintage contradiction follows, and none of these scenarios is a revision test.

6. **Claim: the return and baseline legs cannot establish alpha on the supplied sample. Attack: replace missing executable returns with the existing response columns or score no-revision outcomes as zero. Result: survived.** `abnb_earnings_reactions.csv` has **23 rows**, with 1/5/20-day response columns, **zero `open_*` columns and zero 60-day columns**. These fields do not meet the specified executable-open convention; the 20-day response field is not evidence of a next-open entry. Substitution would change the return object. More fundamentally, the eligibility sample is empty before any return join. A missing FY guide or revision is not a zero revision. Therefore neither the no-revision nor last-revision baseline has a scored pair, and there is no valid RMSE denominator, correlation, hit rate or directional success count.

7. **Claim: registry status and sample-size language do not conceal a successful forecast test. Attack: find an actual `alpha-b` object or treat a schema placeholder/current-date slot as issued forecasts. Result: survived.** The shared registry has **zero `alpha-b__*.csv` files**. The note says none were submitted; the reserved name is not an observation. The frozen README accepts a 2026-09-11 current-date slot and quarterly target format, neither of which authorizes inventing an earlier scenario issuance or storing an annual gap as quarterly revenue. I did not inspect B's schema-only candidate, status JSON or code. Independently established comparator absence is stronger than registry absence alone. W1/W2 apply to this eligibility audit, but n=0 supplies no statistical power or informative directional confidence interval. The conclusion is no validated revision alpha in this test, not proof the kernel has no economic information.

## Every origin and the independently established bound

Each row is one guide origin. Both required historical comparators are missing before every origin; thus the maximum possible eligible count is zero regardless of the unknown kernel signal. The earlier growth-bucket count is included to show exactly where treating narrative centers as dollar guidance would alter the result.

| Guide target | Origin | n | Earlier FY dollar guides | Earlier attributed FY revenue consensus | Earlier FY revenue growth buckets |
|---|---|---:|---:|---:|---:|
| 2023Q1 | 2023-02-14 | 1 | 0 | 0 | 0 |
| 2023Q2 | 2023-05-09 | 1 | 0 | 0 | 0 |
| 2023Q3 | 2023-08-03 | 1 | 0 | 0 | 0 |
| 2023Q4 | 2023-11-01 | 1 | 0 | 0 | 0 |
| 2024Q1 | 2024-02-13 | 1 | 0 | 0 | 0 |
| 2024Q2 | 2024-05-08 | 1 | 0 | 0 | 0 |
| 2024Q3 | 2024-08-06 | 1 | 0 | 0 | 0 |
| 2024Q4 | 2024-11-07 | 1 | 0 | 0 | 0 |
| 2025Q1 | 2025-02-13 | 1 | 0 | 0 | 0 |
| 2025Q2 | 2025-05-01 | 1 | 0 | 0 | 0 |
| 2025Q3 | 2025-08-06 | 1 | 0 | 0 | 0 |
| 2025Q4 | 2025-11-06 | 1 | 0 | 0 | 0 |
| 2026Q1 | 2026-02-12 | 1 | 0 | 0 | 0 |
| 2026Q2 | 2026-05-07 | 1 | 0 | 0 | 1 |
| W1 | 2023Q1–2026Q2 | 14 | 0 | 0 | 1 origin only |
| W2 | 2024Q1–2026Q2 | 10 | 0 | 0 | 1 origin only |

## What ran, limitations and source fingerprints

All commands ran from the repository root using the project `.venv` Python. Only this new note was written. Inputs were the named guidance ledger, frozen calendar, L0 consensus register and earnings-reaction CSV. Source-comment lines beginning `#` were removed before parsing the register; its data-row count at audit is 161, including 12 relevant FY revenue rows. No B or K0 source, B derived output, scenario helper, test suite, scorer or other package result was read or executed. No regression or forecast coefficient was fitted. The live numerator comes from the note and is explicitly not independently regenerated.

Source reads used `Get-Content -LiteralPath`; filename discovery used `rg --files "data"` with source-name filters. The principal independent command below exited **0**, **0.326s shell wall time / 0.0064s computation**. An exploratory ledger/register schema inspection exited 0 (0.357s / 0.0046s computation). A primary-letter read attempted `Path(...).read_text()` for `data/raw/letters/4Q25_d58192dex991.htm` and failed with FileNotFoundError, exit 1 (0.829s). Follow-up source-name discovery found no matching raw files, and `Path.exists()` confirmed that this file plus `1Q26_d23351dex991.htm` and `2Q26_d70413dex991.htm` are all absent. The latter check exited 0 (0.905s); the `rg` no-match exit was 1. This failed primary-source check is not reported as validation. No outside letter or new consensus vintage was substituted. Total session wall time and token usage are unavailable and not estimated.

The missing raw letters limit independent confirmation of their underlying prose. They do not change the complete ledger's absence of a numerical FY dollar-guide row or the consensus register's future dates—the sufficient facts establishing the original supplied-data claim. Model-level completeness and K0 scenario provenance require a separate permitted audit; this note makes no assertion that they were independently replicated.

| Source | SHA-256 |
|---|---|
| `data/processed/overnight/02_guidance_ledger.csv` | `c1a2b34330dc6655c549cbe7cab66adabcaaffcffc4441c833c726714d8267f2` |
| `data/processed/forecast_methods/harness/calendar.csv` | `8433249c908b2adf1a18e9d47aa8b81a0759471ef592aa926c4050a45880efe2` |
| `data/processed/forecast_methods/L0/L0_vintage_register.csv` | `c6402930b7e785a5c24829c6e124023525d3a4be64f33f41ad0ef394cb5af3de` |
| `data/processed/abnb_earnings_reactions.csv` | `45f368e6f10150a481929f199fc1d430a52d6f40e9dd6f5485b661ffa6448b87` |

Exact primary PowerShell command:

```powershell
$env:PATH = (Join-Path (Get-Location) '.venv\Scripts') + [IO.Path]::PathSeparator + $env:PATH
@'
import csv,collections,datetime,decimal,time,hashlib
from pathlib import Path
D=decimal.Decimal;started=time.perf_counter()
def read(p):
    with open(p,encoding='utf-8-sig',newline='') as f:return list(csv.DictReader(line for line in f if not line.startswith('#')))
l=read('data/processed/overnight/02_guidance_ledger.csv')
c=read('data/processed/forecast_methods/harness/calendar.csv')
v=read('data/processed/forecast_methods/L0/L0_vintage_register.csv')
r=read('data/processed/abnb_earnings_reactions.csv')
g=sorted((x['next_quarter_guided'],x['guide_date']) for x in c if '2023Q1'<=x['next_quarter_guided']<='2026Q2')
assert len(g)==14 and sum(q>='2024Q1' for q,d in g)==10
fy=[x for x in l if x['target_period'].startswith('FY')]
dollar=[x for x in fy if x['metric']=='revenue_usd_m' and x['value_mid']]
rev=[x for x in v if x['period'].startswith('FY') and x['metric']=='revenue']
valid=[x for x in rev if x['pit_usable'].lower()=='true' and x['vendor_attributed'].lower()=='true' and x['vendor'] and x['as_of_timestamp'] and x['value']]
print('FY_LEDGER',len(fy),'numeric_dollar_guides',len(dollar),'growth_buckets',sum(x['metric']=='revenue_yoy_pct' for x in fy))
print('REGISTER',len(v),'FY_revenue',len(rev),'attributed_usable',len(valid),'FY_periods',sorted(set(x['period'] for x in rev)),'earliest',min(x['as_of_timestamp'] for x in valid))
for q,d in g:
    year='FY'+q[:4]
    den=[x for x in dollar if x['target_period']==year and x['print_date']<d]
    cons=[x for x in valid if x['period']==year and x['as_of_timestamp'][:10]<d]
    buckets=[x for x in fy if x['target_period']==year and x['metric']=='revenue_yoy_pct' and x['print_date']<d]
    print('ORIGIN',q,d,'FY_dollar_denominators',len(den),'FY_consensus',len(cons),'earlier_growth_buckets',len(buckets))
for w,gs in [('W1',g),('W2',[(q,d) for q,d in g if q>='2024Q1'])]:
    qualified=sum(any(x['target_period']=='FY'+q[:4] and x['print_date']<d for x in dollar) for q,d in gs)
    consensus=sum(any(x['period']=='FY'+q[:4] and x['as_of_timestamp'][:10]<d for x in valid) for q,d in gs)
    false_consensus=sum(any(x['period']=='FY'+q[:4] for x in valid) for q,d in gs)
    print('WINDOW',w,'n',len(gs),'eligible_upper_bound',qualified,'consensus_origins',consensus,'false_date_ignored_consensus_origins',false_consensus)
    for horizon in [30,60,90]:
        pairs=0
        for q,d in gs:
            endpoint=(datetime.date.fromisoformat(d)+datetime.timedelta(days=horizon)).isoformat()
            earlier=any(x['period']=='FY'+q[:4] and x['as_of_timestamp'][:10]<d for x in valid)
            later=any(x['period']=='FY'+q[:4] and d<x['as_of_timestamp'][:10]<=endpoint for x in valid)
            pairs+=earlier and later
        print('CONSENSUS_REVISION_PAIRS',w,horizon,pairs)
print('RETURN_ROWS',len(r),'OPEN_COLUMNS',[x for x in r[0] if x.startswith('open_')],'60_DAY_COLUMNS',[x for x in r[0] if '60' in x])
print('REGISTRY_FILES',list(Path('data/processed/forecast_methods/registry').glob('alpha-b__*.csv')))
for period,point in [('2026Q4',D('3214.78')),('FY2026',D('14309.14'))]:
    candidates=[x for x in v if x['period']==period and x['metric']=='revenue' and x['pit_usable'].lower()=='true' and x['vendor_attributed'].lower()=='true' and x['as_of_timestamp'][:10]<'2026-09-12']
    latest={}
    for x in candidates:
        if x['vendor'] not in latest or x['as_of_timestamp']>latest[x['vendor']]['as_of_timestamp']:latest[x['vendor']]=x
    for vendor,x in sorted(latest.items()):
        cons=D(x['value']); gap=100*(point/cons-1)
        print('LIVE_NOTE_POINT_ARITHMETIC',period,vendor,x['as_of_timestamp'],cons,'gap_pct',gap,'age_days',(datetime.date(2026,9,12)-datetime.date.fromisoformat(x['as_of_timestamp'][:10])).days)
print('Q1_2027_REVENUE_CONSENSUS',sum(x['period']=='2027Q1' and x['metric']=='revenue' for x in v))
for p in ['data/processed/overnight/02_guidance_ledger.csv','data/processed/forecast_methods/harness/calendar.csv','data/processed/forecast_methods/L0/L0_vintage_register.csv','data/processed/abnb_earnings_reactions.csv']:
    print('SHA256',p,hashlib.sha256(Path(p).read_bytes()).hexdigest())
print('seconds',round(time.perf_counter()-started,4))
'@ | python -
```

## Interpretation

The strongest attempted rescue was to interpret the growth-bucket centers as numerical guidance and reuse September FY consensus. Both require changing the data's meaning or date. The permitted, correctly dated inputs leave no eligible test even under a hypothetical perfect kernel. The exact headline counts require no correction. The empty test does not establish no effect, and the positive live arithmetic gaps do not establish a predictive effect.

## RESUME

Parent should count B vintage as **survived**, retaining the exact supplied-data and strict-pre-guide qualifications. A future study needs explicit FY guide/consensus definitions, historical release vintages, a complete dated GBV/kernel information set and executable return fields; archived letter files would enable primary-text verification of the current ledger. Keep any qualitative-guide or post-letter alternative as a separately pre-registered object rather than replacing this empty test. No current consensus or scenario should be backdated. Parent owns board, scoring and git; this agent wrote only the requested refutation note.
