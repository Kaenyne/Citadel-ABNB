# SC-C consumption contract: preregistered audit

2026-09-14 UTC. Claim: WORKBOARD_L3_SOURCE_CONTRACT_v1.md. This is a classification and interface audit, with no forecast, refit, registration, valuation or investment adoption. The original September 13 bundle remains frozen.

Before executing classification tests, require exactly one output row per original bundle row, in original order, with every original string cell preserved, including blank values, units, bounds, source references, source dates, baseline identifiers and treatment. Append a stable source-row digest and explicitly distinguish current exhibit use from insertion into the existing L4 model. Unknown metrics or evidence/treatment combinations fail closed. All six allowed statuses must be reported even when their count is zero: usable_as_observed_input, usable_as_conditional_scenario, descriptive_only, comparator_only, unavailable, rejected. Rejected means unusable evidence; a failed research test remains valid descriptive evidence of failure.

Conditional status permits a labelled scenario exhibit; it does not certify an empirical forecast or successful machine application. List the exact missing baseline, denomination, timing, exposure, hedge, fee and adoption inputs for each route. No reference-FX level factor can overlay reported USD revenue. Missing fee/hotel values remain blank. No RNPL stock share is substituted for a surviving revenue-producing cohort share. Do not add different representations of one adjustment or treat bounds as probabilities. Preserve free-weight promotion FAIL and the operational fixed 2/3–1/3 policy; W2 is nested in W1.

Inspect L4 at actual commit 29b2d9ae2d66da5f1f5086ba7dc36e4c2d96ab70 through shared Git objects. Freeze compact extracts, Git object IDs, SHA-256 hashes and interface facts. Offline reproduction must use that frozen evidence, never a moving external worktree. Keep the current L4 loader's declared provenance checks separate from verified commit membership and root FX quote metadata. Preserve the distinction between revenue consensus and expectations for management's guide.

At actual execution time, inventory existing sanctioned fee-panel files and available local ABNB primary disclosures in both the original workspace and L3 worktree. Freeze paths, timestamps, hashes and comparisons with starting committed bytes. Do not launch, wait for or modify a collector, scrape Airbnb, refresh consensus or reopen hotel/NCLH research without material new data. An empty new-data search is a bounded observation, not proof that no public disclosure exists anywhere.

Acceptance: original row/cell conservation; exact six-status vocabulary; explicit direct-model blockers and conditional dependencies; source/hash integrity; unknown-schema rejection; blank-preservation; negative-result and units semantics; reproducible new-directory-only runner. Reviewer B audits C; C reviews A. This preregistration introduces no empirical pass line and changes no prior result.

## RESUME

Run the consumption package after its frozen evidence inputs are prepared, execute its focused schema/accounting tests, and preserve any failed verification. Deliver classification, compact committed L4 audit, bounded capture inventory and exact reproduction commands to the parent for integration and independent review.
