# GBV research Git delivery — 15 September 2026

User explicitly requested committing and pushing the completed research. This delivery packages the preceding GBV event, cohort, joint-model and decision-audit sessions together because their code, reports and reproducibility receipts reference one another. It preserves earlier failures and superseded notes, with canonical entry points identified below.

Branch: `codex/submission-readiness-v1`. Before publication, fetched `origin` and fast-forwarded from `2dfe0c2a` to `a3af33ee`, incorporating the already merged margin-build PR56. There were no incoming/untracked path collisions and no changes to this audit's shared forecasting code or inputs. This synchronization is not an independent re-audit of the new margin package; the GBV report retains its stated research scope and source cutoffs.

## Read these first

- Decision and prioritized next work: `GD_DECISION_REPORT_v2.md`.
- Completed work/validation index: `../WORKBOARD_GBV_DECISION_COMPLETE_v1.md`.
- Independent review: `GD_REVIEW_RESULTS_v2.md` and `GD_REVIEW_INTERVAL_CLOSURE_v1.md`.
- Rounding sensitivity and open versioned scorer request: `GD_GUIDE_INTERVAL_RESULTS_v1.md`, `GD_GUIDE_INTERVAL_HCR_v1.md`.
- Final visual pack: `outputs/gbv-decision-20260915-v4/GBV_decision_visuals.pdf` from the repository root.

Earlier workbook/two-pager and figure versions are historical research drafts. They are retained for traceability, not declared final submissions or current central-thesis recommendations. This push does not change the live team model, force a long/short direction, or merge into main.

## Publication integrity

An independent publication review found no prohibited licensed exports, credentials or raw reservation/review stores in the scoped new research files. The workbook is generated from derived research tables and has no external-link parts, embedded objects or VBA. Runtime dependencies and caches remain ignored. No teammate message is sent by this delivery.

Git's automatic line-ending conversion changed the would-be object bytes of audited CSVs. Scoped root `.gitattributes` entries disable text normalization only for the new GBV code, results, registry objects, notes and dated artifacts, preserving their recorded hashes. Forecast equations, inputs, results, earlier protected files and frozen scorers are not edited. Staged blob bytes are checked against the exact working-file bytes before committing.

The prior completed numerical and scientific validation remains documented in the canonical workboard. Delivery validation rechecks staged publication scope, bytes, test suites and both unchanged scorer outputs against the completed328-row/144-file checkpoint. No new model registration or fitting is performed. Git/PR receipts and the exact final commit are reported with the push.

## RESUME

Review the new branch/PR using the canonical decision report and final visual pack. Preserve the conditional positive revenue evidence and failed broader guidance gates. The next analytical work remains the common-date Excel model and bounded expectations/provenance/input pilot; the separately merged margin work has not been scientifically reassessed in this delivery. Merge requires the repository's teammate review.
