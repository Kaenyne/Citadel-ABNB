# GD-HORIZON — standalone eligibility sensitivity, before its calculation

15 September2026. The declared main comparison uses common rows for joint, fixed, direct-guide-growth and revenue-growth, ensuring a fair four-model ranking. Initial results show a favorable fixed p+4 finding on9 rows. However, that common sample inherits the joint model's8-row training warmup. The simpler fixed model may be eligible at additional earlier origins. A conditional common-sample pass must not silently become a claim covering all of the fixed model's usable history.

Before computing this sensitivity, freeze a separate eligibility audit: for each candidate (joint or fixed), select every origin where that candidate **and both simple baselines** are available, without requiring the other candidate. Keep original W1/W2 endpoints, all existing points, identical paired guide-error statistics, the2000 deterministic year-cluster draws, year/quarter deletions and original promotion thresholds. No refits, parameter changes, new baselines, source updates or calendar changes. Publish exactly which rows are added relative to the primary common-four sample.

Retain the original common-sample result even if this audit disagrees. An unqualified fixed-model recommendation must disclose and survive its own eligible history; otherwise support is conditional on the shorter shared sample. This is an availability-scope check triggered by the methodology, not a search for a favorable date range. Existing history has been inspected; neither arm is an independent holdout.

## RESUME

Run the standalone eligibility sensitivity from saved `horizon_v1/results_v2/predictions.csv`, publish differences and reconcile any conflicting gate outcomes in the final horizon note. Do not overwrite the primary output or its favorable result.
