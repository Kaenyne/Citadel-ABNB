# REFUTE B2 vintage — preregistration

Codex B2 vintage refuter · 2026-09-13T17:42:00Z · `codex/lane2-full`.

Claim: “The point-in-time kernel correctly signed next-quarter consensus revisions in 5/9 strong-signal cases in W1 and 4/7 in W2, below the pre-registered 70% threshold, while mean signal-aligned 20- and 60-day executable excess returns were negative in both windows.”

## Verdict

PENDING. This records the attacks before running the independent reconstruction.

## Pre-registered pass line

Not applicable — the refuter's output is the verdict. A refuter that only praises has failed. "Partial" does not count as survived.

The exact descriptive sentence survives only if independently dated inputs reproduce its two fractions and four negative means, without current/post-letter consensus or future kernel information. The underlying B2 research threshold stays fixed at |S1| > 0.5%, sign accuracy ≥70%, n ≥6 and correlation >0.4 in BOTH windows. No threshold tuning.

Attacks: (1) rebuild every consensus role, timestamp, vendor and PIT flag; (2) distinguish missing/quarantined rows and zero revisions; (3) reconstruct the kernel using independently truncated KPI inputs and imported lambda, and manually reconstruct trailing-eight median cushions; (4) substitute next-letter lambda, then next-quarter GBV, then both plus next-letter cushion, to test whether look-ahead is needed; (5) independently join executable return components and recompute aligned means; (6) distinguish pre-letter-to-next-print revisions from changes wholly after trade entry; (7) audit source hashes and PIT registry horizons. A synthetic post-close selector challenge is a robustness diagnostic, not evidence that a used historical row was post-close.

Only new `refute_b2_vintage_v1/` code and outputs and companion final note will be written. No registrations or scorer runs are necessary for this read-only audit. The parent owns the shared WORKBOARD update.

## RESUME

Run the independent audit and publish all failed and successful attacks in `REFUTE_B2_vintage.md`, including exact commands, counts, timestamps and limitations.
