# Independent source-package interpretation review for L4 v2

14 September2026. Reviewer: financial/artifact worker C, separate from source worker A. Scope: read-only code/accounting/economic interpretation of `lane4_sources_v2/run.py`, `eligibility.py`, the verified snapshot and source note. No research model was refitted, source file edited, or investment conclusion adopted.

**PASS: no blocking finding within this scope.** The source package distinguishes exact immutable-bundle acceptance from financial eligibility. Its Git batch extraction preserves raw bytes; expected108-file manifest and lineage are checked. CRLF/LF-only historical source bindings are explicitly labeled while canonical bundle hashes stay exact. Duplicate source identities/unknown metrics fail; all1,187 source rows retain measurement/disposition/eligibility and hash fields.

The accounting interface correctly distinguishes backward target-revenue contribution weights from forward cohort allocation, USD-booking FX already in GBV, reference-level T/R0 from replacement T/B, and conditioned RNPL/recognition exposures from observed balances. `prehedge_transform` requires positive matching certified pre-hedge denominator/cohort basis and explicit signed H/H_new before evaluating m*(R-H)+H_new. Null is rejected rather than zero-filled. Source T/B is not relabeled an identified pre-hedge multiplier. Source arithmetic is checked within$1 export precision, not treated as structural validation.

Current source quarter coverage is Q3 only: five L4 reported-Q3 kernel identities match; Q4/Q1 are blocked from extrapolation. All financial FX applications remain ineligible because denominator certification/H splits and measured exposures are absent. ADR scenario totals replace complete inputs; embedded component rows cannot be added again. Fee theta is unavailable, hotel prices comparators and NCLH transfer rejected for ABNB adjustment. Completed free-weight promotion failure preserves the existing K0 estimator; all22free/fixed OLS parameters remain descriptive.

Limit: this independent review validates source-contract logic and interpretation, not a rerun of all research tests or full primary-source measurement. A's18tests and125-file deterministic reconstruction and parent's independent hashes are separate receipts. The model/memo were independently reviewed by A/B/parent; this note is not self-approval of financial artifacts.

## RESUME

Retain this separation of source integrity, assumed-input arithmetic and financial eligibility when a new bundle arrives. A matching reported kernel does not certify pre-hedge accounting, and additional target quarters need their own cohort/reference inputs. Any previously unknown metric or availability change requires a new explicit source review rather than silent schema acceptance.
