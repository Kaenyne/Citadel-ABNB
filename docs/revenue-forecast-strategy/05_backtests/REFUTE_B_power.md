# REFUTE B power — independent eligibility and uncertainty audit

Codex refuter · 2026-09-12 · branch `codex/lane1-full` · main audit: 3.643 seconds shell wall time / 0.1729 seconds measured computation. Token usage unavailable; not estimated.

## Verdict

**survived** — the exact sentence is supported by an independent audit of the calendar, both supplied guidance ledgers, the FY consensus register and the return-file schema. W1/W2 contain 14/10 historical opportunities but zero FY dollar-midpoint guide denominators and zero strictly earlier attributed FY revenue consensus observations. These missing inputs alone establish zero eligible tests without relying on B's derived audit tables or recreating K0. The supplied return file also lacks executable-open and 60-day fields.

The strongest challenge was to promote three FY2026 growth-language records, nine current vendor comparisons or potential 14/10 retrospective rows into validation observations. That fails: the records are different objects or later vintages, and repeated panels do not create independent forecast outcomes. The live arithmetic can reproduce while predictive power remains unmeasured.

## Pre-registered audit

Original exact sentence: “At strict pre-guide dates, the supplied data yield zero eligible FY-guide-revision tests in W1 (0/14) and W2 (0/10), so the live kernel–consensus gap is a conditional scenario, not validated revision alpha.”

Before calculation: rebuild the historical opportunity counts from the frozen calendar, inspect raw FY-guide and attributed consensus coverage, search for an actual registered B forecast, and distinguish missing tests from failed predictions. Count all disclosed specification and sensitivity axes; calculate only statistically meaningful uncertainty at the actual eligible n. Challenge whether live vendor/scenario counts could supply missing historical power. At least five numbered attacks will state claim, attack, explicit result and evidence, ending with exactly survived, refuted or partial on the original sentence. There is no promotion pass line for this refuter.

Read only the assignment, B note, actual B registry files if present, named raw inputs and permitted harness/cheatsheet. No B or other package code or derived output table will be read. Write only this note; parent owns board, scorer and git.

## Numbered refutation attempts

### 1. Claim → W1 has 14 opportunities and W2 has 10. Attack → an off-by-one calendar interpretation or inclusion of August LIVE may change the denominators.

**Result: survived.** I selected raw frozen-calendar rows by `next_quarter_guided` in the stated target windows and a non-null guide date. This gives:

| Window | Audited origins n | First origin | Last origin | Eligible FY dollar-guide denominators n | Earlier attributed FY consensus n |
| --- | ---: | --- | --- | ---: | ---: |
| W1, targets 2023Q1–2026Q2 | 14 | 2023-02-14 | 2026-05-07 | 0 | 0 |
| W2, targets 2024Q1–2026Q2 | 10 | 2024-02-13 | 2026-05-07 | 0 | 0 |

The 2026-08-06 event targets 2026Q3 and is excluded by this selection. W2 is exactly the later subset of W1; 14 + 10 is not 24 unique origins or independent confirmation. These denominators count opportunities to build tests, not completed predictions or successes.

### 2. Claim → no eligible FY-guide-revision test exists. Attack → the FY ledger has 47 rows and three apparent revenue revisions.

**Result: survived.** The raw FY ledger has 47 rows across ten metrics, but only three revenue rows. Filtering the general guidance ledger independently produces the same three records and no FY revenue dollar-midpoint guide:

| Target | Date | Raw guide type / unit | Encoded low / mid / high | n |
| --- | --- | --- | --- | ---: |
| FY2026 revenue growth | 2026-02-12 | bucket / pct | 10 / 11 / 12 | 1 |
| FY2026 revenue growth | 2026-05-07 | bucket / pct | 12 / 14 / 16 | 1 |
| FY2026 revenue growth | 2026-08-06 | bucket / pct | 14 / 15 / 16 | 1 |

The first and third quotes are open-ended “at least” language, and the middle quote describes a broad growth range. Their encoded centres are not issued dollar midpoint guidance or letter-rounded integer measurements. Applying ±0.5 to those centres would replace qualitative bucket semantics with a fabricated measurement model.

Only one origin in each nested window has an earlier current-FY revenue bucket: 7 May 2026 can see the February FY2026 bucket. It still has no issued dollar midpoint denominator for the pre-registered `|T / FY guide midpoint| > 0.5%` condition. The two adjacent changes in encoded centres, +3pp and +1pp, are two descriptive transitions in one fiscal year, with the August event LIVE. They cannot be scored as the requested annual dollar-guide tests without a separately specified conversion sensitivity and a valid signal.

The independent absence of the required denominator is sufficient for zero eligibility. It does not claim that Airbnb issued no FY language or that no alternative, explicitly qualitative study could ever be designed.

### 3. Claim → strict pre-guide data do not support the consensus-revision alternative. Attack → historical quarterly consensus, current FY vintages or vendor repetitions may supply pairs.

**Result: survived.** The raw L0 register contains **12 FY revenue entries**, six FY2026 and six FY2027. All are marked attributed and PIT-usable as dated entries, but the earliest timestamp is **2026-09-03**. That is after the final historical origin, 2026-05-07. For each of the 14/10 origins, filtering for the applicable FY, attributed usable value and timestamp strictly earlier than the origin yields zero rows.

Being usable at a later date is not being available at an earlier one. Quarterly consensus cannot be silently relabelled annual; a current FY value cannot supply either the pre-guide comparator or a historical 30/60/90-day revision endpoint. Repeated September observations for a vendor are not a long historical revision series. Same-day inclusion would not repair this particular sample: the FY entries are months later, not just a few hours late.

No B registry file exists under its reserved `alpha-b__` method. This supports the note's abstention but is not itself the reason for zero eligibility; the missing raw denominator/comparator is the substantive barrier.

### 4. Claim → n=0 is insufficient to establish revision alpha. Attack → report a zero hit rate, bootstrap the empty sample, or infer power from the 14/10 opportunity counts.

**Result: survived.** Actual eligible forecast/outcome n is **zero**. Hit rate, correlation, Wilson interval, paired-loss bootstrap and minimum detectable forecasting effect are undefined. [0,1] remains the unrestricted set of possible hit probabilities, not an estimated 95% interval. A no-revision baseline has no common scoreable sample either; its error is not zero and no RMSE ratio exists.

For scale only, I independently calculated the following **hypothetical benchmarks if all 14/10 opportunities eventually became independent, eligible binary forecasts**. These are not B performance estimates and are not replacements for the pre-registered economic thresholds:

| Hypothetical independent n | Exact one-sided 5% rejection of p=0.5 | Power if true hit rate is 70% | True hit rate required for 80% power | Wilson 95% interval for an observed rate near 70% |
| ---: | --- | ---: | ---: | --- |
| 14 | at least 11 hits | 35.52% | 83.12% | 10/14: [45.35%, 88.28%] |
| 10 | at least 9 hits | 14.93% | 91.67% | 7/10: [39.68%, 89.22%] |

A Fisher-z approximation for a two-sided 5% correlation test requires a true correlation of about **0.688 at n=14** or **0.785 at n=10** for 80% power against zero. This approximation assumes independent pairs and ignores selection/overlap. The observed-correlation gate of >0.4 is an economic pass threshold, not proof of conventional significance. The |T| filter could further reduce any future sample. None of these hypothetical calculations confers power on the present n=0 test.

### 5. Claim → the live kernel–consensus gap remains a conditional scenario. Attack → multiple positive vendor comparisons may independently validate the signal.

**Result: survived.** I independently selected the latest strictly pre-12-September row per exact vendor label and recomputed `(displayed kernel point / consensus − 1) × 100`. The numerator is the B note's reported scenario, not a K0 forecast independently rebuilt in this audit. Values below are therefore verified **gap arithmetic on reported scenario inputs**:

| Period / reported kernel input | Exact vendor label | Timestamp | Consensus, $M | Recomputed gap | n |
| --- | --- | --- | ---: | ---: | ---: |
| 2026Q4 / $3,214.78M | S&P Global Market Intelligence via StockAnalysis | 2026-09-10 | 3,160 | +1.73354% | 1 |
| 2026Q4 / $3,214.78M | Alpha Vantage (aggregated sell-side panel) | 2026-09-11 | 3,158 | +1.79797% | 1 |
| 2026Q4 / $3,214.78M | Yahoo Finance | 2026-09-11 | 3,160 | +1.73354% | 1 |
| 2026Q4 / $3,214.78M | Zacks | 2026-09-11 | 3,200 | +0.46188% | 1 |
| FY2026 / $14,309.14M | S&P Global Market Intelligence | 2026-09-03 | 14,160 | +1.05325% | 1 |
| FY2026 / $14,309.14M | S&P Global Market Intelligence via StockAnalysis | 2026-09-10 | 14,160 | +1.05325% | 1 |
| FY2026 / $14,309.14M | Alpha Vantage (aggregated sell-side panel) | 2026-09-11 | 14,155 | +1.08894% | 1 |
| FY2026 / $14,309.14M | Yahoo Finance | 2026-09-11 | 14,160 | +1.05325% | 1 |
| FY2026 / $14,309.14M | Zacks | 2026-09-11 | 14,100 | +1.48326% | 1 |

The selected observations are 1–9 days old at the stated as-of date. The register has no 2027Q1 revenue consensus row. These nine rows describe two scenario numerators against repeated or related panels; they are not nine independent realised forecast outcomes. The two S&P labels remain separate for provenance, not evidence of independent research panels. Alpha Vantage is not relabelled as another vendor.

The displayed Q4 numerator is rounded to $0.01M, so it cannot reproduce every final decimal of a gap calculated from an unrounded K0 point. In particular, +1.73354% from $3,214.78M can straddle the note's three-decimal rounding boundary. That is not evidence of a materially different gap. More precise scenario arithmetic cannot establish future revisions, appropriate timing or returns.

### 6. Claim → reported specifications do not constitute a validated search result. Attack → count all thresholds, horizons, scenarios and model parameters as if they independently strengthened the case.

**Result: survived.** The note discloses the following axes; no scoreable result exists on any of them:

| Disclosed axis | Settings / comparison count | Actual eligible historical pairs n |
| --- | --- | ---: |
| Primary guide-revision gate | One 0.5% magnitude threshold; 70% sign and >0.4 correlation gates; two nested windows | 0 |
| FY-consensus revision horizon | 30 / 60 / 90 days; 60 days is the stated secondary test | 0 |
| Return horizon | 20 / 60 days in each window | 0 |
| Guide-revision baselines | No revision; last revision continues | 0 |
| Live kernel horizons | 2026Q4 / 2027Q1 | 0 validated revision outcomes |
| Live fixed-lambda weight sensitivity | At least two disclosed endpoint weights, 0.33 and 2/3; exact internal grid count not disclosed | 0 |
| Current vendor comparisons | Four Q4 rows and five FY2026 rows | 0 realised revision outcomes |

These settings overlap, and I do not add them into an artificial independent-trial n. Nor can the full development-search count be certified from the allowed note/inputs. B adds zero fitted coefficients, but that does not mean its forecasts are parameter-free: it inherits up to four seasonal conversion coefficients, the note's two live ledger-regression coefficients and an uncertainty model. Fixed RNPL ramps and persistence of a second missing quarter's GBV are substantive assumptions even if not estimated by B.

The reported Q4 and Q1 lambda support is five and six seasonal observations respectively. The live descriptive q10–q90 widths are $301.72M and $569.20M; their fixed-lambda weight ranges span only $30.40M and $123.73M. These are different sensitivity objects, not interchangeable confidence bounds. Their exact K0 draws and empirical coverage were not re-estimated in this audit. Holding the fitted coefficient fixed while changing lag weights cannot be presented as a refitted family of historical models.

### 7. Claim → return outcomes and inherited K0 machinery do not rescue the empty annual test. Attack → use available close returns or skip the missing annual components.

**Result: survived.** `abnb_earnings_reactions.csv` contains **23 event rows**, but its columns are 1-, 5- and 20-day ABNB/QQQ/relative returns. There are **zero `open_*` columns and zero 60-day columns**. No executable-open return leg can be constructed from this specified input without another data source. Close returns would answer a different execution question and were not substituted.

I did not re-run K0 or independently certify its claim of zero complete four-quarter historical FY forecasts; doing so would require package internals or another package's derived state, excluded by the assignment. The missing FY-guide denominator and strict pre-origin FY consensus already establish zero eligible tests even if one imagined a complete kernel numerator. A prospective missing-GBV forecast method or an explicitly post-letter information set could alter a future study, but neither can be inserted silently into the present strict pre-guide test. LSEG history alone would not validate an undated historical GBV-nowcast path or supply executable returns.

## What ran, commands and boundaries

Named raw inputs inspected: `data/processed/overnight/02_fy_guide_revisions.csv`, `02_guidance_ledger.csv`, `data/processed/forecast_methods/L0/L0_vintage_register.csv`, `data/processed/abnb_earnings_reactions.csv`, and the frozen `harness/calendar.csv` named by the permitted harness README. Registry inspection was filename-only because no `alpha-b__*.csv` file exists. No B code, K0 code, B audit/output table or other package's derived forecast output was read. No network data, consensus refresh, registration or scorer run was needed.

The audit was pre-registered in this new note before numerical work. The command below ran from the repository root using the existing project `.venv`, exit 0, **3.64265 seconds shell wall time / 0.17287 seconds measured computation**. It independently reproduces the counts and gap/statistical arithmetic reported here. The original K0 dollar forecasts and their uncertainty distributions remain reported scenario inputs; this note does not claim an independent forecast-engine rebuild.

```powershell
& ".venv/Scripts/Activate.ps1"
@'
import json,time
from pathlib import Path
import pandas as pd
import numpy as np
from scipy.stats import binom,norm
from scipy.optimize import brentq
s=time.perf_counter()
calendar=pd.read_csv('data/processed/forecast_methods/harness/calendar.csv')
calendar['origin']=pd.to_datetime(calendar.guide_date,errors='coerce')
fy=pd.read_csv('data/processed/overnight/02_fy_guide_revisions.csv')
fy['d']=pd.to_datetime(fy.print_date)
ledger=pd.read_csv('data/processed/overnight/02_guidance_ledger.csv')
register=pd.read_csv('data/processed/forecast_methods/L0/L0_vintage_register.csv',comment='#')
register['stamp']=pd.to_datetime(register.as_of_timestamp,format='mixed',utc=True,errors='coerce')
fyc=register[register.period.str.startswith('FY') & register.metric.eq('revenue')]
guides=fy[fy.metric.str.contains('revenue',case=False)]
dollar=guides[guides.unit.isin(['usd_m','musd','USD millions','USD_m']) & guides.value_mid.notna()]
print('FY_LEDGER',len(fy),'metrics',fy.metric.value_counts().to_dict(),'revenue_rows',len(guides),'dollar_mid_rows',len(dollar))
print('FY_REVENUE_GUIDES',guides[['target_period','metric','print_date','guide_type','value_low','value_high','value_mid','unit','quote']].to_json(orient='records'))
print('GENERAL_LEDGER_FY_REVENUE',ledger[ledger.target_period.str.startswith('FY') & ledger.metric.str.contains('revenue',case=False)][['target_period','metric','guide_type','unit','print_date']].to_json(orient='records'))
print('FY_CONSENSUS',len(fyc),'period_counts',fyc.period.value_counts().to_dict(),'earliest',str(fyc.stamp.min()),'flags',fyc.groupby(['pit_usable','vendor_attributed']).size().to_dict())
for name,start in [('W1','2023Q1'),('W2','2024Q1')]:
    origins=calendar[calendar.next_quarter_guided.between(start,'2026Q2') & calendar.origin.notna()]
    cons_counts=[]; dollars=[]; buckets=[]
    for _,row in origins.iterrows():
        stamp=row.origin.tz_localize('UTC'); annual='FY'+row.next_quarter_guided[:4]
        cs=fyc[fyc.period.eq(annual)&fyc.pit_usable&fyc.vendor_attributed&fyc.value.notna()&(fyc.stamp<stamp)]
        cons_counts.append(len(cs))
        dollars.append(len(dollar[dollar.target_period.eq(annual)&(dollar.d<row.origin)]))
        buckets.append(len(guides[guides.target_period.eq(annual)&(guides.d<row.origin)]))
    print('WINDOW',name,'n',len(origins),'first',str(origins.origin.min()),'last',str(origins.origin.max()),'consensus_eligible_origins',sum(x>0 for x in cons_counts),'dollar_denominator_origins',sum(x>0 for x in dollars),'earlier_bucket_origins',sum(x>0 for x in buckets),'origins',origins.guide_date.tolist())
reaction=pd.read_csv('data/processed/abnb_earnings_reactions.csv')
print('REACTIONS','rows',len(reaction),'open_columns',[c for c in reaction if c.startswith('open_')],'60day_columns',[c for c in reaction if '60' in c])
print('REGISTRY',[p.name for p in Path('data/processed/forecast_methods/registry').glob('alpha-b__*.csv')])
asof=pd.Timestamp('2026-09-12',tz='UTC')
live=register[register.period.isin(['2026Q4','FY2026','2027Q1'])&register.metric.eq('revenue')&register.pit_usable&register.vendor_attributed&register.value.notna()&(register.stamp<asof)]
print('LIVE_REGISTER_INPUTS',live[['register_id','period','vendor','as_of_timestamp','value','role']].to_json(orient='records'))
latest=live.sort_values('stamp').groupby(['period','vendor'],sort=False).tail(1)
points={'2026Q4':3214.78,'FY2026':14309.14}
for _,row in latest.iterrows():
    if row.period in points: print('LIVE_GAP',row.period,row.vendor,row.as_of_timestamp,row.value,100*(points[row.period]/row.value-1),'age_days',(asof-row.stamp).total_seconds()/86400)
for n in [14,10]:
    cutoff=next(k for k in range(n+1) if binom.sf(k-1,n,.5)<=.05)
    p80=brentq(lambda p:binom.sf(cutoff-1,n,p)-.8,.5,1-1e-9)
    k=int(np.ceil(.7*n));phat=k/n;z=norm.ppf(.975);den=1+z*z/n
    c=(phat+z*z/(2*n))/den;h=z*((phat*(1-phat)/n+z*z/(4*n*n))**.5)/den
    print('HYPOTHETICAL_POWER',n,'cutoff',cutoff,'power_at70',binom.sf(cutoff-1,n,.7),'p80',p80,'Wilson_if_near70',[c-h,c+h],'correlation80_Fisher_approx',np.tanh((norm.ppf(.975)+norm.ppf(.8))/np.sqrt(n-3)))
print('LIVE_INTERVALS',{'Q4_width':3357.90-3056.18,'Q1_width':3396.50-2827.30,'Q4_weight_width':3245.18-3214.78,'Q1_weight_width':3245.15-3121.42})
print('elapsed_seconds',time.perf_counter()-s)
'@ | python -
```

## Interpretation

This is a data-sufficiency conclusion, not proof that a kernel revision strategy has no economic value. The supplied inputs cannot evaluate the pre-registered strategy. Zero eligible tests must remain unavailable metrics, not zero errors or failed predictions. The exact 0/14 and 0/10 statement survives independent input checks; no headline correction is required. The live scenario can be reviewed arithmetically without being promoted to validated revision alpha.

## RESUME

Parent can record **survived** on B's exact original sentence. Preserve the independent 14/10 origin counts and the two decisive missing inputs: issued FY dollar-midpoint guide denominators and strict pre-origin attributed FY consensus. A next version needs a clearly defined annual-guide object, archived FY consensus/revision vintages, complete dated kernel inputs and executable-open return data before attempting power or baseline comparisons. Any bucket-to-dollar conversion or post-letter study must be explicitly pre-registered as a new design. Only this assigned note was written; parent owns board, scorer and git.
