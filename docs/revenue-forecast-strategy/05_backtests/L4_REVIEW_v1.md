# L4 review memo, unsigned November card and quantified decisions

Codex subagent committed_evidence_audit; 13 September 2026; branch
`codex/lane4-full`; starting evidence commit
`1c87628cedbc94ab8a0e8552743c94485ef353b8`. New package
`analysis/src/forecast_methods/lane4_review_v1/`, new data outputs and
`deck/drafts/lane4_v1/` only. Actual token usage unavailable.

## Verdict

**Implementation PASS; independent parent review pending. Research conclusions
are inherited and no investment adoption occurs.** The final editable Markdown
renders into exactly two readable pages at 10.5pt body size. Both final pages were
visually inspected. Sixteen tests pass, seven valuation rows and 21 annual
cash-flow rows reconcile, and the review's guide matches the final revenue
snapshot. The fixed 2/3 K0 is expressly a **benchmark**, provisional pending L3
conversion validation; FX/RNPL integration is separately pending and not treated
as zero. Direction, target, probabilities and formal card adoption remain open.

## Preregistration and scope

This implements `LANE4_PREREG_v1.md` and the user's Prompt 2 review/memo/card
request. The PDF and equity-pitch skills were read before authoring; the PDF
artifact-start marker succeeded once before the first authoring operation.
User instructions to leave direction, target and probabilities open supersede
the generic equity template's recommendation/probability requirements.

The initial subtask was a read-only committed-evidence audit. Memo production was
temporarily stopped at the user's priority change, preserving all completed
files. A subsequent bounded conversion-method scoping read verified data/schema
and existing code without fitting or executing a model. After the user's role
correction, L4 resumed integration only: L3 owns conversion estimation/validation.
No new conversion estimate or statistical result was generated by this package.

## Authoritative inputs and output versions

The final builder copied and SHA-256 verified explicit immutable snapshots:

- `data/processed/forecast_methods/lane4_revenue_v1/snapshot_v1/`
- `data/processed/forecast_methods/lane4_model_v1/snapshot_v1/`
- The committed ADRv3, A2/B2/F, V, card-definition and convention sources listed
  individually in the input manifest.

Final outputs are under `deck/drafts/lane4_v1/review_v1/`:

| Output | Count / scope | Standing |
|---|---:|---|
| `review_memo.pdf` and editable `review_memo.md` | Exactly 2 pages | Review draft, unsigned |
| `unsigned_november_card.md`, `.csv`, `.json` | 11 diagnostic rows | Exact definitions, units, sources, rounding, absent/ambiguous outcomes |
| `decision_register.md`, `.csv` | 23 quantified choice rows, 10 L4 decision groups | All adoption open; no probabilities invented |
| `source_assumption_ledger.csv` | 437 metric/assumption records | Units, dates, status and source references |
| `input_manifest.json` in data run | 29 input/source records | Byte-verified snapshot hashes |

`data/processed/forecast_methods/lane4_review_v1/review_v1/` contains snapshots,
source manifest, numerical consistency checks, artifact hashes, test receipt,
rendered page PNGs and `visual_QA.json`. The earlier `layout_01` run is preserved
as a layout prototype built with model `development_run_02`; it is superseded by
`review_v1` and is not the delivery version.

## What ran

Commands were run from the isolated L4 worktree root. `python` below denotes the
bundled interpreter at
`C:/Users/wille/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe`.

```powershell
python -m unittest discover -s analysis/src/forecast_methods/lane4_review_v1/tests -v
python analysis/src/forecast_methods/lane4_review_v1/run.py --revenue-dir data/processed/forecast_methods/lane4_revenue_v1/snapshot_v1 --model-dir data/processed/forecast_methods/lane4_model_v1/snapshot_v1 --run-id review_v1
```

Sixteen tests passed (unittest 0.025 seconds), followed by builder exit 0 and a
two-page result. The combined tool cell took 2.5358105 seconds. An existing run
ID is intentionally refused; reproduction must use a **new** run ID.

Final rendering, using bundled Poppler:

```powershell
pdftoppm -r 120 -png deck/drafts/lane4_v1/review_v1/review_memo.pdf data/processed/forecast_methods/lane4_review_v1/review_v1/page
```

Renderer exit 0; combined rendering/metadata read tool cell 1.0931016 seconds.
Both final PNGs were inspected with `view_image`. Layout has no clipping,
overlaps, missing glyph boxes or broken table/page boundaries. Poppler emitted
display-font warnings for Symbol and ArialUnicode; the ASCII-text output renders
without a visible defect. Body 10.5pt, notes/sources 9pt. PDF SHA-256:
`15cb6eff8ee28b83a81cce13656846807c202c179d1a05929296b33ef9cbf6f5`.

## Results and decision-useful limits

All dollar values below are USD millions unless stated. Current scenarios are
conditional benchmark calculations, not PIT alpha results or adopted forecasts.

| Reconciliation | n | Result |
|---|---:|---|
| Reference Q4 guide | 1 scenario | 3,123.41911517339 |
| Latest captured Yahoo/LSEG comparison | 1 panel | 3,161.02149, 13 Sep 2026 15:20 UTC; gap -37.602375, -1.189564% |
| Other independent panels | 2 panels | S&P 3,160 on 10 Sep; Zacks 3,200 on 11 Sep |
| Reference FY27-end EBITDA lens | 1 scenario | 184.662755252916 USD/share at 16.5x; 31 Dec 2027 convention |
| Legacy arithmetic | 1 base scenario / 6 dependent lenses | EBITDA lens 180.876286; six-lens mean 156.786845; distinct legacy convention |
| Valuation reconciliation | 7 scenario rows | EV, net cash, equity and per-share value agree within 0.001 |
| Cash-flow reconciliation | 21 scenario/year rows | EBITDA-to-FCF and FCF-less-SBC identities agree within 0.001 |
| Q4 cohort contribution conservation | 2 lag rows | Sum 1.0000000000000009; dollar weights, not measured recognition probabilities |
| Original D-01 Q3 benchmark | 1 current benchmark | Revenue 4,808.362929 |
| Original D-01 already-issued guide comparators | 2 methods | Median 4,814.690226 (+6.327296); mean 4,817.823947 (+9.461017) |

The main exhibit presents operating, fee-mechanics, residual and cushion
comparisons with the same units and panel stamps. All conversions remain
provisional. The model's +/-1% rows affect only Q3/Q4 2026 and Q1 2027 directly;
FY27 Q3/Q4 inherit growth on the perturbed 2026 base, and FY27 Q2 is unchanged.
They are financial-response sensitivities, not L3 FX estimates or a +/-1%
full-FY27 revenue shock. The PDF states this scope and the EBITDA-to-cash-flow
equation, including its inherited tax/capex assumptions and SBC-adjusted FCF.

The memo preserves A2 PARTIAL and B2 FAIL, their small nested samples and lack of
an established executable edge. It excludes B2's unseasonal LIVE extrapolation.
ADRv3's n=10/9 windows are distinct from the main harness n=14/10; its post-hoc
rule selection and failed both-window integer-fair ex-FX acceptance remain
visible. F's corrected stock history is 2.0/8.0/9.7pp; migration and revenue-cohort
RNPL shares remain unidentified. The +0.48 growth/multiple relation is excluded
from the valuation mechanism.

The card retains the fixed denominator 27,866.666666... and controlling lambda
cutoffs 17.09/16.93%, corresponding to 4,762.413333... / 4,717.826666... USDm.
Whole-million rounding is interval-scored. F's conjunction is a proposed
refutation condition only; missing observations remain ABSENT and overlapping
intervals remain ambiguous/inconclusive. Funds-payable 5-11% is a neutral band,
not proof of deferral. No card output authorizes a trade. New L4-Dxx decision IDs
do not overwrite the original numbering; L4-D09 explicitly preserves original
D-01's Q3 revenue comparison.

## What failed or could not be done

No package test or builder failed in the recorded final run. The final L3
conversion and cohort FX/RNPL inputs have not been supplied; no measured change,
central estimate, confidence level, expected return or investment adoption is
manufactured. This affects conversion-dependent interpretation, not the ability
to build and audit a labelled benchmark package. Numerical completion and visual
quality do not turn inherited partial/failed evidence into an investment pass.

Free fitted parameters: **0**. Card cutoffs, operating assumptions and valuation
inputs are inherited/proposed choices, not parameters estimated by this package.
No frozen source, existing protected note/model/data, registry or scorer output
was modified by this subagent. Parent owns publication, registrations and scores.

## RESUME

Parent should independently inspect both `review_v1` PDF pages, check the
benchmark/FX status language and compare memo/card/decision numbers with the two
final upstream snapshots. Review the 29 hashes and `visual_QA.json`; `layout_01`
is historical only. If a demonstrated error is found, preserve this run and
rebuild to a new ID. Accepted L3 conversion or FX/RNPL inputs require an explicit
version/commit and checksums plus a reviewed new integration version; this
pending-input renderer intentionally refuses to imply that integration happened.
Leave all direction/target/probability/card choices unsigned for the team.
