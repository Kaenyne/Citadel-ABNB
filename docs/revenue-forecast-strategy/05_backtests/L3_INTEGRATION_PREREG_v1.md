# L3 integration preregistration v1

2026-09-13 · before L3 scorer and integration tests. Starting hash receipt covers all 3,911 base-commit files and the explicit external FX dependency (4,046 files total). Never rewrite a base-commit file. Protect every initial source hash through close; new packages only.

Run the actual frozen FORMAT 1.0 scorer followed by FORMAT 1.1, redirecting only their output destinations using the already audited Lane 2 helper. Save new snapshots. Compare every key, count, flag, string and missing value exactly to Lane 2's committed close scoreboard (284 rows); floating metrics absolute error strictly <1e-9, rtol zero. Compare both new scorers to one another under the same rule. No L3 registry writes or new forecasts.

All package tests must pass; research outcomes may fail or be partial. Run the existing frozen/L0, FORMAT 1.1, kernel and returns test suites once at close. Independent agents review packages they did not author for provenance, economic/accounting identification, leakage and exposure/FX double counting. Preserve their first findings and fixes as new receipts.

Bundle acceptance: every L4 row has quarter, metric, scenario, value and bounds (null where unidentified), units, information date, evidence status, source reference, treatment and replacement baseline. Reject nonfinite numeric values, invalid dates and duplicate semantic keys. Separate unverified research and conditional scenarios from admissible operational quantities. No schema-valid scenario constitutes investment adoption. Bundle manifests hash all payload files; construction refuses an existing destination. Reproduction uses a new output path and compares semantic results where runtime stamps differ.

## RESUME

Run l3_integration_v1 score snapshot and checks after package reviews. Validate source hashes, build an immutable L4 bundle, review scoped git diff, commit and publish codex/lane3-full. Leave team adoption and merge pending.
