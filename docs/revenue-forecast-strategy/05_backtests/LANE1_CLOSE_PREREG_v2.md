# Lane 1 v2 close — pre-registration

Written before package A returns and before any refuter runs. Close requires an
exact sentence, number, reproducible evidence path, limitation and W1/W2 support
for every surviving memo claim. Each headline from A, B, R, V and X is challenged
by separate vintage, power and mechanism agents with at least five explicit
attacks each. Only two or more explicit survived verdicts admit the exact
sentence; partial verdicts do not count as survived. Corrected sentences require
clear separation from the original disputed sentence, not silent substitution.

The final scorer must run after the last registration. Frozen scorer source and
outputs are preserved through the byte-verified temporary-copy adapter in
`analysis/src/forecast_methods/lane1_control_v2/run.py`. The before-A receipt
contains 4,109 registry rows, 69 objects, 276 scored groups, exit 0 in 19.46 s.
Compare later leaders against this freshly computed baseline, not stale prose.
Missing ratio denominators are labelled **no baseline exists**. Identical
ten-quarter W1/W2 samples are labelled **vacuous**. Same-day frozen baseline
forecasts do not establish a strict pre-guide comparison against new methods.

The final report must distinguish implemented calculations from identified
investment effects, and describe whether X changes the Q3/Q4 FX conclusion or
merely generates an assumption-dependent scenario. B4 is superseded only by
supported replacement evidence. No direction, target, fee migration, RNPL ramp,
FX step or other reserved team decision will be adopted.

All package pass lines, results, three refuter verdicts, Gate 1/Gate 2 receipts,
actual token-accounting availability and unfinished work belong in the PR body.
No registered forecasts, licensed raw data, personal files or environment files
may be fabricated or accidentally included. Parent reviews the staged path list
and frozen-file diff before every checkpoint. One branch, one requested PR to
main, no main-branch commits.
