Read `C:\Users\krish\citadel-abnb-overnight\docs\overnight\00_BRIEF.md` first and follow it. You are workstream **15: Red-team verification of the overnight workstreams 01-12.**

For each note in `research/notes/overnight/01..12_*.md`: (1) pick the 5-8 most consequential numeric claims in its bottom line and "For the model" section; (2) verify each against the primary source (the letter HTML in `data/raw/letters/`, the XBRL JSON, the CSV the note cites, the URL it cites via WebFetch, or a re-run of its script with `py -3.13`); (3) check the statistics: n, whether leave-one-out / walk-forward was actually done as claimed, whether the test count is stated, whether the window avoids the 2021-22 base effects and the 2023 normalisation trend, whether point-in-time availability holds; (4) look for internal contradictions between notes (e.g. different FY27 revenue growth, ADR, FX contribution, take rate, share count, regional shares, margin scenarios, consensus numbers across WS02/04/05/07/10/12) and list every disagreement with the values and which one is better supported; (5) flag any licensed-content quoting beyond ~25 words from the Third Bridge PDFs; (6) run each script once to confirm it executes end-to-end and regenerates its CSVs (record failures).

Output `data/processed/overnight/15_claim_checks.csv` (workstream, claim, value_claimed, value_verified, source_checked, verdict: confirmed / wrong / unsupported / unverifiable, note) and `15_cross_note_conflicts.csv` (parameter, ws_a, value_a, ws_b, value_b, recommended, reason), `15_script_runs.csv`.

Note `research/notes/overnight/15_red-team.md`: bottom line (how many claims checked, confirmed, wrong, unsupported; the conflicts that matter for the model), per-workstream findings, a "corrections to apply" list with exact file paths and replacement values, and a short list of claims that should not appear in the pitch. Do not edit other notes; the synthesis workstream will apply your corrections. Final message: 300-500 word summary with the counts and the top corrections.
