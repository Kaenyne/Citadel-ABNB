Read `C:\Users\krish\citadel-abnb-overnight\docs\overnight\00_BRIEF.md` first and follow it. You are workstream **13: Build the driver model (Excel with live formulas plus a Python mirror) that integrates every workstream's "For the model" parameters.**

Inputs: the twelve notes `research/notes/overnight/01..12_*.md` (read each note's "For the model" section and its key tables), plus `research/notes/2026-09-05_driver-model.md`, `2026-09-05_margin-drivers.md`, `2026-09-05_capital-return-panel.md`, `model/assumptions.md`, and CSVs `data/processed/overnight/02_kpi_panel_quarterly.csv`, `07_margin_levers_fy26_fy28.csv`, `10_regional_forecast.csv`, `05_macro_scenarios.csv`, `05_fx_schedule.csv`, `11_new_business_scenarios.csv`, `11_regulatory_overlay.csv`, `12_*` multiples, `04_consensus_at_print.csv` (Street FY26/FY27), `abnb_capital_return_quarterly.csv`.

Deliver:
1. `model/ABNB_driver_model.xlsx` built with openpyxl (`py -3.13`) using real Excel formulas (not pasted values) on these sheets: `Inputs` (every scenario parameter with a source cell and a bear/base/bull selector), `History` (quarterly 1Q21-2Q26 KPIs and cost lines from the panel), `Revenue` (quarterly 3Q26-4Q27 and annual FY26-FY28: nights by region x growth, ADR ex-FX, FX contribution by quarter from the FX schedule, GBV, take rate incl. timing, new-business revenue, regulatory drag; revenue = sum), `Costs` (lever-by-lever: payment cost ratio, ops & support per night, product dev ex-SBC, S&M cash vs revenue, G&A, new-business investment, SBC by line, D&A; adj. EBITDA, GAAP operating income, net income with interest income and tax), `Cash` (FCF bridge, buybacks, withholding, share count path, FCF/share, SBC-adjusted FCF/share), `Valuation` (EV/EBITDA, EV/FCF, P/SBC-adj FCF, P/E-proxy, DCF with the cost of equity from WS09/12, exit multiples from WS12; football field; implied upside per scenario; reverse DCF), `Street` (consensus FY26/FY27 vs our base with the deltas), `Card_5Nov` (the Q3 2026 print prediction: revenue, nights, ADR, margin, guide expectations with the ranges from WS02/04/08). Use named ranges or a clean Inputs block so a teammate can flex assumptions. Seasonality: derive quarterly shares from history.
2. `analysis/src/overnight/13_driver_model.py`: the same model in pandas, producing `data/processed/overnight/13_model_quarterly.csv`, `13_model_annual.csv`, `13_valuation_summary.csv`, `13_scenario_grid.csv` (revenue growth x margin x multiple → price), and a reconciliation check that the Python and Excel base cases agree within rounding (write the check to `13_reconciliation.csv`; open the xlsx with formulas evaluated via a LibreOffice headless recalculation if available, otherwise compute expected values in Python and compare cell inputs; state which).
3. Update `model/assumptions.md`: append a dated section "Overnight run 6-7 Sep 2026" listing every changed/added assumption with source workstream, without deleting existing rows.
4. Note `research/notes/overnight/13_driver-model-build.md`: bottom line (base/bear/bull FY26-FY28 revenue, EBITDA, FCF, FCF/share, price per lens; what changed vs the 5 Sep driver-model note and why), the input table, the seasonality and FX mechanics, the quarterly path 3Q26-4Q27, what the model cannot do yet, "For the 5 Nov card".

Final message: 300-500 word summary with headline numbers and file paths.
