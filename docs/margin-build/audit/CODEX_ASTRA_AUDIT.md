Verdict: **Not ready to quote as an audited forecasting model.**  
The conditional 3Q26 point—49.94% margin and $2,399M EBITDA—reproduces.  
Fix the longer-horizon timing leak and reconcile the cost, EPS and FCF bridges first.  
Withdraw the 77% beat probability and unsupported coverage claims pending consistent calibration.  
The calculations below were reproduced read-only; a complete build and scorer run remain unverified.

Paths below are relative to the repository. `F/` means `analysis/src/margin_build/23_final_model/`; `D/` means `data/processed/margin_build/23_final_model/`. Dollar amounts are USD millions unless stated otherwise.

| id | severity | file:line or CSV cell | what is wrong | how you verified | proposed fix |
|---|---|---|---|---|---|
| 01 | critical | `F/combine.py:262`, `:285` | **h=1/h=2 weights and intervals use actuals unavailable at the vintage.** Earlier target quarters are selected by quarter order, without a print-date gate. For example, forecasting 2023Q3 on 9 May includes 2023Q2 errors, although that quarter printed 3 August. | Replayed the original functions in memory: all **1,056 rows** matched committed points within `2.3e-13`. Then added `print_date <= vintage_date`. W1 h=1/h=2 points change by up to **0.184/0.250pp**; h=2 coverage falls from **10/12 to 9/12**. h=0 is unchanged. Primary pass/fail conclusions survive this correction. | Gate both member-error and combination-error pools by publication date. Apply the same rule in `diagnostics.py`; rebuild and re-score. |
| 02 | major | `F/forecast.py:158`, `:170`, `:224`; workbook `Bridge!B4:B10` | **The displayed cost stack does not sum to its total.** Five 3Q26 cash lines total **2,437.418**, while “Total cash costs” is **2,404.903**. The missing **32.515** is `other_net`, available in the Lines CSV but omitted from the quarterly P&L and Bridge display. | Summed the five CSV/workbook lines. Verified the actual identity is `EBITDA = revenue − sum(lines) + other_net`, consistent with M1’s definition. | Display the reconciliation explicitly, separating D&A and other adjustments. Label the net cost total accordingly; add an independent reconciliation check. |
| 03 | major | `F/forecast.py:150`, `:210`; `D/23_lines_quarterly.csv`, 2026Q3/base | **The cost and GAAP bridges assume different add-backs.** The cost stack carries `other_net=32.515`, including D&A, but the GAAP bridge subtracts only **20.634 D&A** and SBC, effectively assuming other add-backs are zero. | Computed `other_net − D&A`: **11.881** in 3Q26. If the cash stack is authoritative, operating income is overstated by that amount and EPS by **$0.01647**. All six base forecast quarters have a positive discrepancy. | Use one adjustment schedule in both bridges. Either remove unsupported other add-backs from the cost reconciliation or subtract them in the GAAP bridge. |
| 04 | major | `F/forecast.py:493`; `D/23_bands.csv`; `SYNTHESIS.md:14` | **Dollar accuracy, the displayed interval and the beat probability describe different forecasts.** The adopted dollars are the six-member margin forecast × revenue; the separately scored dollar combination has four members. Its narrower error scale supplies the published probability. | The displayed **2,299.196–2,499.069** band implies Gaussian SD **77.978** and beat probability **68.52%**. The code uses SD **51.954**, producing **76.54%**. Separately replaying the adopted dollar construction gives MAE **30.66/25.84**, versus the quoted standalone-dollar **39.68/30.86**, W1/W2 n=14/10. | Register and calibrate the exact adopted forecast. Keep the dollar combination as a separately labelled cross-check. **68.52% is only the band-consistent Gaussian calculation, not a validated replacement probability.** |
| 05 | major | `F/forecast.py:145`; `D/23_forecast_quarterly.csv`; `D/23_scenarios.csv` | **Bear/bull financial statements do not use the stated cost-response scenarios.** Quarterly margins are fixed across revenue scenarios; reconciliation forces costs to accommodate that result. The scenario engine separately applies elasticity 0.364. | For 3Q26, P&L bear/base/bull margins are all **49.9399%**. The flex scenario gives **49.6117/49.9399/50.4226%**. Bear EBITDA differs by **15.608**; bull by **23.543**. | Feed one selected cost-response scenario into the quarterly and annual statements, or label the existing statements explicitly as constant-margin revenue sensitivities. |
| 06 | major | `F/forecast.py:238`; `D/23_forecast_quarterly.csv`, `eps_q10/eps_q90` | **The advertised EPS interval excludes below-EBITDA uncertainty.** Tax, SBC, interest and share-count forecasts are treated as certain, despite M7’s measured bridge errors. | The 3Q26 EPS half-width **$0.138502** equals exactly `EBITDA half-width × (1−ETR)/shares`. No bridge residual enters the calculation. | Propagate matched EBITDA and bridge errors jointly, accounting for covariance, or label these as conditional EBITDA-only sensitivities. |
| 07 | major | `F/forecast.py:216`, `:288` | **Updating EBITDA changes FCF pretax while net income changes after tax.** The copied M7 cash-flow bridge therefore ceases to reconcile with the new earnings forecast unless an offsetting cash-tax adjustment is assumed. None is supplied. | Compared the implemented `ΔFCF=ΔEBITDA` with M7’s `CFO=NI+D&A+SBC+working capital+other`. The omitted tax effect is **−15.698** for 3Q26 and **−13.566/−13.798** for FY26/FY27 base. | Rebuild CFO from the updated net income and cash-flow components. If cash taxes intentionally remain fixed, show the explicit reconciling adjustment. |
| 08 | major | `F/combine.py:472`; `data/processed/margin_build/registry/final-margin__combined.csv` | **Consensus provenance disappears in the final registry.** The composite consumes raw Street and M5 forecasts, but does not propagate vendor or consensus vintage. This defeats FORMAT 1.0’s required audit trail for consensus-consuming objects. | Counted **768 h=0/h=1 rows**; every `street_vendor` and `street_as_of` is blank. All **1,152** final rows also lack `knowable_from`. | Preserve member identifiers, vendor stamps and maximum input availability date. Classify the composite as consensus-anchored; distinguish it from the existing Street-independent variant. |
| 09 | major | `F/combine.py:472`, `:487`; `SYNTHESIS.md:474` | **Registry complexity and training counts have the wrong meaning.** `n_params=2` reports only the wrapper’s additions; `n_train` records member count, not observations. The quotable statement repeats the two-parameter claim. | Registry values are always **2 parameters** and **3/4/6 training observations**. `prereg.json` itself acknowledges **52 inherited parameters plus 2 additions** for the h=0 margin construction. | Publish wrapper and inherited complexity separately. Populate observation counts according to FORMAT 1.0, with a separate `n_members` field. |
| 10 | major | `SYNTHESIS.md:155`, `:160`; `F/prereg.json` | **Post-selection evidence is overstated.** The pool was chosen after inspecting the scoreboard. Small PIT/full-sample differences and leave-one-member-out stability do not demonstrate that selection bias is negligible. | Compared the acknowledged selection process with the replay: pool membership remains fixed in every historical replay; no historical selection step or untouched evaluation set is used. M3’s included `nov_sentence_pin` is explicitly marked post-hoc in its source. | Describe the results as retrospective, conditional on the selected pool. Freeze the model for prospective evaluation; do not use hindsight-share or ablation results as proof against selection bias. |
| 11 | major | `F/forecast.py:121`; `SYNTHESIS.md:61`, `:347` | **“Attained coverage 82–91%” is not measured coverage.** Those endpoints are an order-statistic grid calculated from sample size. They do not establish future coverage for selected, expanding-window time-series forecasts. | At n=10, the code emits **9/11 and 10/11** directly. Actual registered h=0 margin coverage is **13/14 W1 and 10/10 W2**, using different historical calibration pools. | Separate theoretical rank resolution, observed coverage and exchangeability assumptions. Label the live interval descriptive until prospectively validated. |
| 12 | major | `SYNTHESIS.md:374`; `D/23_card_budget_identity.csv` | **An unchanged FY floor is treated as a point forecast and trading signal.** “At least 35.5%” implies a lower bound on Q4 margin, not that Q4 will equal 27.6% or that the print must be a sell. | Recomputed the budget identity: FY margin **≥35.5%** implies Q4 margin **≥approximately 27.6%**, conditional on the stated revenue and Q3 assumptions. The adopted **28.90%** Q4 already satisfies it. | Preserve the inequality. Separate a guide-floor calculation from a forecast of the actual and from any independently tested price reaction. |
| 13 | major | `SYNTHESIS.md:21`, `:481`; `M6_cycle_flex_peer_k.csv`, ABNB/opex_total/rw | **“Airbnb has no cost dial” confuses imprecision with zero responsiveness.** The insignificant elasticity cannot establish that costs do not respond. | Recomputed approximate normal uncertainty from **k=0.13914, t=0.46792, n=18**: SE ≈0.2974 and 95% interval ≈**−0.44 to +0.72**, encompassing substantial positive flexibility. The model separately uses cash-cost elasticity **0.364**. | Say this regression does not precisely identify aggregate flexibility. Keep held-cost and estimated-flex scenarios; distinguish their cost definitions. |
| 14 | minor | `F/forecast.py:330`; `D/23_vs_consensus.csv`, `lseg_sd_musd` | **One dispersion column changes definition between quarterly and annual rows.** Quarterly entries are revenue SD; annual entries are EBITDA SD, beside EBITDA counts and comparisons. | Traced the assignment to `revenue_sd_musd`. For 3Q26/4Q26 it supplies **24.276/48.559**, rather than the synthesis’s EBITDA SDs of approximately **20.0/26.5**. | Split revenue and EBITDA dispersion into explicitly named columns and update the workbook. |
| 15 | minor | `F/forecast.py:283`; `D/23_forecast_annual.csv`, FY26/net_income_musd | **FY26 net income does not sum to quarters.** An effective tax rate from the old M7 EBITDA mix is reapplied to the new annual pretax amount. | Base annual NI is **3,145.928** versus **3,146.125** from H1 actuals plus forecast quarters. Bear/bull discrepancies are **−0.300/−0.065**. | Sum quarterly tax provisions and net income; derive annual ETR afterward. |
| 16 | minor | Workbook `Inputs`, `Bridge!B3`, `Bridge!B26`; `F/workbook.py` | **The workbook is principally a snapshot, not an input-driven model.** Inputs do not feed forecast cells, annuals are pasted values, and formula results have no saved caches. | Inspected with `openpyxl`: all **42 formulas** are local Bridge formulas; none references another sheet. All 42 cached results are blank. Independently evaluating the formulas reproduces CSV values. | Label the workbook as a frozen report, or connect inputs, costs and annual sums with formulas. Recalculate and save caches before distribution. |
| 17 | minor | `F/combine.py:474`, `:489`; final registry `notes` | **Registry notes contain a Python method representation instead of the clipping status.** `r.clip` resolves to the Series method. | All **1,152 notes** contain `bound method`; inspected the embedded Series representations. | Use `r["clip"]`. |
| 18 | minor | `F/run.py`; `analysis/src/margin_build/10_harness_margin/score.py` | **End-to-end reproducibility was not established in this audit environment.** This is an execution limitation, not proof that the committed results are wrong. | Attempted both requested `python …` commands: Windows launchers were inaccessible. The accessible bundled interpreter ran both entry points but each stopped with **`ModuleNotFoundError: scipy`**, before output writes. Read-only AST replay and independent pandas/openpyxl calculations succeeded. | Verify in the declared environment with dependencies installed. Add a no-write verification mode so the build and scorer can be audited without overwriting outputs. |

**What to keep**

- The h=0 margin replay reproduces: **MAE 1.126/0.788pp**, recency-weighted **0.823/0.662pp**, W1/W2 n=14/10. The date-gate correction leaves it unchanged. W1’s sign-test p=0.003174 correctly uses **11 wins, one loss and two excluded ties**.
- All **18 forecast revenue rows** exactly match WS06 v2b. FY26/FY27 revenue and EBITDA annual totals reconcile within source rounding. The reported **0.628%/0.937%** held/flex revenue shortfalls reproduce the 35.5% floor.
- The workbook faithfully reproduces its source outputs: **174 quarterly bridge cells** and **1,759 additional table cells** checked, with no material differences. The defects above originate in the shared calculations.
- Keep the explicit failed-test disclosures, FY27 scenario label, FY28 extrapolation label and quarterly-FCF restriction. Recorded date fields across **33 registry files, 86,468 rows** show no later-than-vintage violations; that does not cure missing provenance or internal calibration leakage.

**Three highest-value improvements under an hour each**

1. **Repair the print-date gates and replay.** Update both calibration pools and diagnostics, regenerate scores, and replace the affected h=1/h=2 figures.
2. **Unify the accounting bridge.** Carry D&A and other add-backs explicitly, update CFO from after-tax earnings, sum annual tax/NI from quarters, and add reconciliation assertions.
3. **Make the card internally consistent.** Calibrate the exact adopted dollar construction; remove the mixed-object 77%, label EPS bands conditional, and rewrite the coverage, floor and complexity claims.