Read-only audit of a small bottom-up margin model. Working directory is the repository root. Use `py -3.13` (has pandas, openpyxl, scipy) to reproduce; the venv `python` lacks them. No network. Do not open data/raw/theo_onedrive or data/raw/licensed.

Read `analysis/src/margin_build/40_line_build/run.py` (about 300 lines) and its outputs in `data/processed/margin_build/40_line_build/` (40_params.csv, 40_lines_quarterly.csv, 40_annual.csv, 40_sentence_implied.csv, 40_backcast.csv, 40_sensitivities.csv). Inputs: `data/processed/margin_build/02_financial_panel/02_panel_quarterly.csv` (actuals; identity adj_ebitda = revenue - cash lines + da + other add-backs), `data/processed/margin_build/06_fy27_path_v2/06_revenue_path_3q26_4q27_v2b.csv` (revenue path).

Check, and reproduce before asserting: (1) arithmetic of each cost line against its stated formula and parameters; (2) the FY26 annual = 1H26 actual + 2H26 build for every line, and FY27 = sum of quarters; (3) the reconciliation step (section 3 of build()): does the 3Q26 base margin land where the code claims, is the gap allocated as documented (marketing Q3-only, hosting run-rate), and does the FY27 marketing base include the Q3 step as the comment says; (4) the ops & support rule and its calibration (v = 0.19) - is the 1H26 arithmetic right; (5) the fee-rate seasonal factors and whether the 1H26 in-sample check uses them consistently; (6) below-EBITDA bridge: op income, interest income, tax, shares, EPS; annual EPS denominators; (7) anything in 40_annual.csv that does not tie to 40_lines_quarterly.csv; (8) the FY28 roll-forward row. Report as a markdown table: id | severity | file:line or CSV cell | what is wrong | how verified | fix. At most 15 findings. Then three sentences on whether the build is fit to quote. Keep it under 900 words.
