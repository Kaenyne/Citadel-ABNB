# A19 (X01) launch prompt — saved for the record 17 Sep 13:15 (the "A19 brief" the X01 rev-1 log cites)

The orchestrator's Agent launch prompt for X01 instructed, verbatim on the definitions:

"Define each option exactly per the question text and resolve the overlaps explicitly: thesis breaker = printed nights ≥10.6% AND
C02 ∈ {(a) low double digits or better}; short case = nights ≤8.5% OR C02 ∈ {(d)} OR C04 ∈ {(d) softer/lower}; base = decelerating
print (<10.6%) AND (C02 ∈ {(b),(c)} OR C01 = below Street) AND not short case; none of the above = everything else (e.g., in-line print
with in-line guide). State the precedence rule you use where two options' conditions both hold (short case beats base; thesis breaker
requires both its conditions)."

Note: the launch prompt's parenthetical "(<10.6%)" for "decelerating print" is the source of audit finding A19-01 (the question text
says decelerating; S01's dead band puts that at <10.09%). The A19 response should treat the question text as authoritative.
