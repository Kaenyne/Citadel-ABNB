export const meta = {
  name: 'archives-and-daio-builds',
  description: 'Three Fable builders in parallel (2023 Inside Airbnb reviews mirror wedge test; older Inside Airbnb archives attrition ladder; EUROCONTROL daio EMEA feature), each followed by an independent verifier',
  phases: [
    { title: 'Build', detail: 'pull the data, copy the pipelines into v2 folders, pre-register, run, write the note' },
    { title: 'Verify', detail: 'one refuting verifier per build' },
  ],
}

const WT = 'C:/Users/krish/citadel-abnb-ghcat'
const MAIN = 'C:/Users/krish/citadel-abnb'
const CAP = 'C:/Users/krish/abnb_ia_capture'
const S = `${WT}/data/processed/github_altdata/samples`
const RULES = `Worktree ${WT} (branch krish/github-altdata-catalog). Rules that bind you: copy never overwrite (never modify anything under analysis/src/q3nowcast/, analysis/src/govdata/, analysis/src/forecast_methods/harness/, data/processed/q3nowcast/, data/processed/govdata/, or any tracked data file; new code goes in a NEW folder with a run.py that exits 0 and a README.md with the command; new outputs in a NEW data/processed folder); point-in-time or it does not count (W1 = 1Q23+, W2 = 1Q24+; a result must survive both to be quoted; vocabulary: survivor / short-window survivor / corroboration / duplicate / fail); pre-register the pass line in the note BEFORE running the test; a failed test is written up, not deleted; raw pulls go under gitignored folders (${WT}/data/raw/<name>/ or ${CAP}/<name>/) with a manifest CSV (url, bytes, sha256, pulled_at) committed under the new data/processed folder; never commit to git (the orchestrator commits); never touch airbnb.com; no credentials, no paid keys. Bash calls time out at 10 minutes: run long downloads as a resumable loop that skips files already present with the right size, in chunks, or with run_in_background. Python is \`python\` (pandas 3, pyarrow, duckdb); run scripts from the worktree root; notes in plain prose, no em-dashes; the note goes under ${WT}/docs/revenue-forecast-strategy/05_backtests/ using the section 7 template of ${WT}/docs/revenue-forecast-strategy/AGENT_BRIEF.md (Verdict first paragraph, What ran with exact commands and exit codes, Results tables with n, What failed, Interpretation, Harness change requests, RESUME). When done append ONE row to ${WT}/docs/revenue-forecast-strategy/WORKBOARD.md (do not rewrite other rows). The lane spec with every detail (feeds, transformation, coverage, pass line, full pull, first command) is in ${WT}/docs/github-altdata/integration_plan_raw.json: read your lane's entries before starting.`

const BUILD_SCHEMA = { type: 'object', properties: {
  build: { type: 'string' },
  data_pulled: { type: 'string', description: 'what, where, bytes, file counts, manifest path, anything missing' },
  pre_registered_lines: { type: 'string' },
  results: { type: 'string', description: 'every test with n and PASS/FAIL against the pre-registered line; the live 3Q26 reading where relevant' },
  files_written: { type: 'array', items: { type: 'string' } },
  note_path: { type: 'string' },
  registry_and_score: { type: 'string', description: 'registry files written (or not, and why); score.py exit code and scoreboard delta if run' },
  problems: { type: 'string' },
  what_the_verifier_should_recheck: { type: 'string' },
}, required: ['build', 'data_pulled', 'pre_registered_lines', 'results', 'files_written', 'note_path', 'registry_and_score', 'problems', 'what_the_verifier_should_recheck'] }

const BUILDS = [
  { key: 'mirror2023', prompt: `BUILD A: the 116-market Mar-May 2023 Inside Airbnb reviews mirror as a second vintage for the reviews stays index, plus the NYC Local Law 18 bracket. Read the 'inside_airbnb_archives' and 'booking_hf' lane entries for slug hf-buenosaires-airbnb-reviews in integration_plan_raw.json, the E pipeline ${WT}/analysis/src/q3nowcast/E3_review_counts.py, E4_build_index.py, E5_backtest.py (E3 globs *_reviews.csv.gz under MAIN/data/raw/inside_airbnb_reviews and derives market_key and dump_date from the stem; it caches per-file daily counts), the note ${WT}/research/notes/q3nowcast/E_reviews-stays-index.md (tables 2.2 attrition and 2.3 posting lag), ${WT}/data/processed/q3nowcast/E/market_geo.csv, and the sample folder ${S}/hf-buenosaires-airbnb-reviews (hf_raw_dl_file_index.csv lists the 116 raw_dl paths; manifest.json).
Steps: (1) Create the note ${WT}/docs/revenue-forecast-strategy/05_backtests/WPK_reviews-index-2023-vintage.md and pre-register: T1 wedge constancy = within-vintage y/y for review months Mar 2022 to Feb 2023 computed inside the 2023 vintage versus inside the held 2025 vintage, global review-weighted |difference| <= 2.0 pp and every region <= 3.0 pp (PASS = the constant-wedge assumption survives a second era; FAIL = the W1 claim of the stays index gets a revintaging caveat and is downgraded to W2-only unless the E5 walk-forward re-run with 2023-vintage values substituted keeps the ratio vs naive <= 0.75); T2 attrition extension = pooled same-month ratio 2025-vintage/2023-vintage at 26 to 40 months of dump age lies in 0.75 to 0.90; T3 NYC LL18 = attrition-corrected NYC review flow Sep 2023 to Aug 2024 versus Sep 2022 to Aug 2023 falls in -40% to -70% (corroborates the CRA -56.1% guest-nights benchmark in ${WT}/research/regulatory/phase2/nyc_activity_benchmark.json). (2) Download all 116 raw_dl files from https://huggingface.co/datasets/alujjdnd/Airbnb-Mar-2022-2023/resolve/main/<path> (git-LFS via resolve, no auth; ~4.88 GB; NYC 132 MB and the 13 panel cities first) into ${CAP}/ia_reviews_vintage2023/ renamed to <market_key>_<dump_date>_reviews.csv.gz where market_key matches market_geo.csv (110 of 116 match the raw_dl stem minus the date; map japan_kantō_tokyo -> japan_kanto_tokyo and the three mangled ireland / malta / new-zealand names by inspection; skip the two 75-byte china files). Resumable loop, verify gzip integrity (python gzip read of the header or gzip -t), write ${WT}/data/processed/q3nowcast_v2/E/raw_manifest_vintage2023.csv (market_key, dump_date, path, bytes, sha256, pulled_at). Never put these files into MAIN/data/raw/inside_airbnb_reviews (that would silently change the v1 pipeline). (3) Copy E3 (and E4 as needed) into ${WT}/analysis/src/q3nowcast_v2/E/ with REV set to BOTH the held folder ${MAIN}/data/raw/inside_airbnb_reviews and the new vintage folder, OUT = ${WT}/data/processed/q3nowcast_v2/E/, and a CACHE that reuses the v1 per-file daily caches by copying them (so only the 2023 files are counted afresh). Run it (run.py, exit 0) producing market_vintage_monthly.csv with the 2023 vintages added. (4) Compute T1, T2, T3 exactly as pre-registered; for T3 also use ${S}/nyc-airbnb-listings-2024-01-05/new_york_listings_2024.csv (reviewed-active listings Jan 2024) and the held NYC 2025-09-01 reviews dump; write data/processed/regulatory_v2/nyc_ll18_bracket.csv (date, basis, listings_reviewed_ltm, min30_share, entire_share, licensed_n, reviews_ltm_sum, review_flow_12m, source). (5) If T1 FAILS, re-run a copy of E5 on the 2023Q1 window with the 2023-vintage values substituted and report the ratio vs naive; do not re-register anything through the harness in this build, write a harness change request instead. (6) Finish the note with all tables (n on every row), the attribution 'Inside Airbnb, CC BY 4.0, dump dates as listed', WORKBOARD row. Budget: the download is the long pole; start it first in the background and do the code copy while it runs.` },
  { key: 'archives_ladder', prompt: `BUILD B: the older Inside Airbnb archives as an attrition ladder for the reviews stays index. Read the 'inside_airbnb_archives' lane entries for slugs montera34-barcelona-insideairbnb-2015-2019, chicagoboothml-insideairbnb-2015-30cities and joeydejager-nyc-2015 in integration_plan_raw.json, the E note ${WT}/research/notes/q3nowcast/E_reviews-stays-index.md (table 2.2 attrition by dump age, currently to ~36 months, pooled ~15%/yr), ${WT}/data/processed/q3nowcast/E/survivorship_wedge.csv and market_vintage_monthly.csv (the held 2025/2026 vintages), market_geo.csv, and the three sample folders under ${S}/.
Steps: (1) Create ${WT}/docs/revenue-forecast-strategy/05_backtests/WPK_reviews-attrition-ladder-2015-2019.md and pre-register: L1 (montera34, 34 Barcelona vintages Apr 2015 to Mar 2019) = 12-month attrition of review history measured on successive vintage pairs in 2017 to 2019 lies in 10 to 20% per year (PASS = the E note's constant-wedge assumption gains an independent era); L2 (ChicagoBooth 2015, 30 cities) = pooled cumulative attrition of 2014 review months between the 2015 vintage and the held 2025/2026 vintage equals (1 - 0.15)^10 = 0.20 within +/- 0.10; L3 (joeydejager NYC 2015-01-01) = the NYC 2015 vs 2025 attrition point is reported and lies within +/- 0.10 of the L2 pooled value. (2) Pull: montera34 via git clone --depth 1 --filter=blob:none --sparse https://github.com/montera34/airbnb.barcelona into ${CAP}/montera34_barcelona then git sparse-checkout set data/original/airbnb (34 folders x listings_summary + reviews_summary, ~90 MB); ChicagoBooth reviews.csv.gz per city from https://raw.githubusercontent.com/ChicagoBoothML/DATA___InsideAirBnB/master/<City%20Name>/reviews.csv.gz into ${CAP}/chicagobooth_2015/ (URL-encode spaces; ~300-400 MB for 30 cities; resumable loop; if the raw endpoint rejects LFS files, fall back to git clone --depth 1 --filter=blob:none --sparse and sparse-checkout the reviews files) plus listings.csv.gz for NYC and Barcelona; joeydejager nyc_reviews_20150101184336.csv from https://raw.githubusercontent.com/JoeyDeJager/inside-airbnb-data/master/new-york-city/2015-01-01/visualizations/ (5.6 MB). Manifest with sha256 under ${WT}/data/processed/q3nowcast_v2/E_attrition/raw_manifest.csv. (3) Write ${WT}/analysis/src/q3nowcast_v2/E_attrition/run.py: for every archive vintage count reviews by listing x month (listing_id, date only), map city directory names to market_key via market_geo.csv (report the unmatched), then compute attrition ratios = same market-month count in the held latest vintage divided by the count in the archive vintage, by months of dump-age difference; output survivorship_ladder.csv (market_key, archive_vintage, held_vintage, review_month, n_archive, n_held, ratio, age_months) and a pooled table by age bucket (0-12, 13-24, ..., 109-120 months) with n and review-weighted ratio. Exit 0. (4) Evaluate L1, L2, L3 against the pre-registered lines. Also produce the Barcelona descriptive exhibit: listing count, entire-home share, multi-listing share, median quoted price (EUR, no FX) per montera34 dump, as barcelona_supply_2015_2019.csv. (5) Note with tables, attribution 'Inside Airbnb, CC BY 4.0', WORKBOARD row.` },
  { key: 'daio', prompt: `BUILD C: EUROCONTROL daio daily flights as an EMEA nights feature. Read the 'emea_volume' lane entry for slug eurocontrol-daio-daily-flights in integration_plan_raw.json and the red-team notes on it (critic.challenges), the govdata V protocol ${WT}/analysis/src/govdata/V1_collect.py, V2_backtests.py, V3_rank.py and its brief ${WT}/docs/govdata/BRIEF.md and outputs ${WT}/data/processed/govdata/V/ (V_backtests.csv, V_feature_panel.csv, raw/eurostat_avia_paoc_monthly.csv), the protocol ${WT}/analysis/src/adrq3/I0_protocol.py (walkforward(min_fit=4), score(), perm_p), the regional panel ${WT}/data/processed/overnight/10_regional_panel_quarterly.csv (quarter like '3Q26'; targets emea_nights_yoy_mid, and total_nights_yoy_pct as the secondary), and the sample ${S}/eurocontrol-daio-daily-flights (daio_2025.csv, daio_2026.csv, SOURCE_README.md, manifest.json).
Steps: (1) Create ${WT}/docs/revenue-forecast-strategy/05_backtests/C2_eurocontrol-daio.md and pre-register BEFORE pulling anything beyond the sample: features eu40_flt_da_full, eu40_flt_da_lag1, eu_core_flt_da_full (EU27+UK+CH+NO; excludes Israel, Morocco, Ukraine, Moldova, Georgia, Armenia, Turkiye, Albania, Serbia, Bosnia, North Macedonia), plus eu40_flt_da_qtd75 as the live-quarter variant (days 1-75 of the quarter vs the same 75 days a year earlier); flt_da = departures + arrivals (overflights excluded); quarterly sums guarded to complete quarters; y/y % vs the same quarter a year earlier; because of min_fit=4 and a target starting 3Q22, W1 wf_n = 12 and W2 wf_n = 10 (write these n down); the 2022 rows are a reopening base effect, so the fit starts at 1Q23 (state this) and a 'vs 2019' variant is reported for 2022 only as a sensitivity; T0 duplicate check against the held Eurostat avia_paoc on the overlapping quarters (correlation of the two y/y series; if r > 0.97 on the overlap the feature is labelled 'duplicate of avia_paoc on history, new for the live quarter'); pass lines in the govdata vocabulary: SURVIVOR = walk-forward RMSE ratio vs naive < 1.0 on BOTH windows with a positive OLS slope on both and wf_n >= 6 on emea_nights_yoy_mid; SHORT-WINDOW SURVIVOR = < 1.0 on 1Q23+ only; else CORROBORATION (it carries a 3Q26 EMEA reading no held series gives). Report permutation p and the jackknife as the protocol does. (2) Pull the eight yearly CSVs https://raw.githubusercontent.com/euctrl-pru/daio/HEAD/daio_2019.csv .. daio_2026.csv into ${WT}/data/raw/eurocontrol_daio/ (gitignored) with a manifest committed at ${WT}/data/processed/govdata_v2/raw_manifest.csv; also git clone https://github.com/euctrl-pru/daio ${CAP}/euctrl_daio_git (full history, small) and use it for two things: (a) a revision check (how much do the last 7 days of a yearly file change between commits a week apart; report the median absolute revision) and (b) a true point-in-time qtd75 reading for each quarter since 2023 by reading the file as of the commit nearest the 75th day, so the live variant has a PIT history. (3) Copy V1/V2/V3 into ${WT}/analysis/src/govdata_v2/ (run.py, README.md), outputs to ${WT}/data/processed/govdata_v2/; build the feature panel by quarter; run the backtests on both windows against emea_nights_yoy_mid (primary) and total_nights_yoy_pct (secondary); rank. (4) Live reading: report 3Q26 qtd75 (1 Jul to 13 Sep 2026 vs the same days of 2025) and the last complete quarters (1Q26, 2Q26) for eu40 and eu_core, next to 2Q26 EMEA nights y/y from the panel. (5) Harness: check whether ${WT}/data/processed/forecast_methods/harness/targets.csv carries an EMEA nights target; if it does and the feature is a survivor on both windows, register eu-flights-v1 through FORMAT 1.0 (read harness/README.md) and run score.py; otherwise write a harness change request in the note and register nothing. (6) Note with the verdict, all tables, the country weights table (share of flt_da by state, 2025) for WP-X, the WP-O line for EUROCONTROL reuse terms (no LICENSE in the repo), and the WORKBOARD row.` },
]

const V_SCHEMA = { type: 'object', properties: {
  lens: { type: 'string' },
  checks: { type: 'array', items: { type: 'object', properties: { check: { type: 'string' }, result: { type: 'string', enum: ['confirmed', 'refuted', 'could_not_check'] }, evidence: { type: 'string' } }, required: ['check', 'result', 'evidence'] } },
  refutations: { type: 'array', items: { type: 'string' } },
  verdict: { type: 'string', enum: ['commit', 'fix_then_commit', 'do_not_commit'] },
}, required: ['lens', 'checks', 'refutations', 'verdict'] }

function verifyPrompt(b, report) {
  return `${RULES}

You are the independent verifier for build ${b.key}. Try to refute it; do not take its numbers on trust. Do not modify any file except scratch scripts under ${WT}/data/processed/verify_${b.key}/ . Checks: (1) \`git -C "${WT}" status --short\` and \`git -C "${WT}" diff --stat\`: no tracked file under analysis/src/q3nowcast/, analysis/src/govdata/, analysis/src/forecast_methods/, data/processed/q3nowcast/, data/processed/govdata/, data/processed/forecast_methods/ may be modified (additions in new folders are fine; WORKBOARD.md may only have added lines; a harness registry/scoreboard change is allowed only if the report says a registration was made). (2) The note exists, has the section 7 structure, states the pass lines in a section that precedes the results, uses the team vocabulary, quotes n on every result row, contains no em-dashes, and claims nothing beyond what the pre-registered line allows (no forecast-edge claim for a corroboration; no number from the kill list in AGENT_BRIEF section 6). (3) Recompute the headline numbers independently from the raw data and the pipeline outputs (at least: the main test statistic per pre-registered line, the n, and one randomly chosen table row), and compare to the note. (4) The raw manifest exists, its sha256 values match the files on disk for at least three files, and no raw file is inside a tracked path. (5) run.py of the new folder exits 0 when you run it (or, if it would re-download gigabytes, that it detects the existing files and skips). (6) Anything the builder asked you to recheck. Verdict: commit / fix_then_commit / do_not_commit with concrete refutations.

BUILD REPORT:
${JSON.stringify(report, null, 1)}`
}

const results = await pipeline(BUILDS,
  (b) => agent(`${RULES}\n\n${b.prompt}`, { label: `build:${b.key}`, phase: 'Build', schema: BUILD_SCHEMA, model: 'fable', effort: 'high' }).then(r => ({ key: b.key, report: r })),
  (r, b) => r.report ? agent(verifyPrompt(b, r.report), { label: `verify:${b.key}`, phase: 'Verify', schema: V_SCHEMA, model: 'fable', effort: 'high' }).then(v => ({ ...r, verify: v })) : r,
)
return results.filter(Boolean)