# Independent ABNB thesis validation

The bounded research is complete when the final acceptance and independent artifact receipts pass. **The forecast promotion hurdle fails.** The package is a frozen-source reconstruction and conditional economics bridge, not a production forecast, original-vintage archive or adopted investment position.

Run from this worktree's root. Python 3.11+ with NumPy, pandas and matplotlib is required for research and charts. A Python with reportlab and pypdf renders the one-page summary; it may be the same executable. Git must retain the L3 and L4 commits below. No network, new fit/search/resample, credentials or licensed data is needed.

```powershell
& 'C:/Users/wille/Desktop/Citadel - ABNB/.venv/Scripts/python.exe' -B -X utf8 analysis/src/forecast_methods/quant_thesis_validation_v1/run.py --run-id final_v1 --pdf-python 'C:/Users/wille/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe'
```

Use a new run-id on every invocation. The runner refuses existing destinations before starting. It resolves repository paths from its own location, confines outputs to the new quant data package, and runs each author and independent numerical review in order. Every subprocess command, runtime version, exit status and log is preserved under `data/processed/forecast_methods/quant_thesis_validation_v1/reproductions/<run-id>/`. A failed attempt remains available and exits nonzero. The prospective and economics adversarial tests run as part of the command. Scientific tables are compared byte for byte to canonical outputs. Generated PDF/SVG metadata is not claimed byte deterministic; the PDF renderer verifies one page and text presence, and the canonical artifact is separately visually reviewed.

The runner rebuilds source/availability checks, accepted uncertainty diagnostics, the locked forecast experiment, expectations, marginal economics, independent accounting and numerical reviews, claims, both figures and the PDF. Reviews read canonical author outputs; the runner also verifies its fresh numerical outputs equal those canonical files. The expectations figure consumes the canonical bridge whose fresh CSV equality is verified. The independent reproduction wrapper performs its own fresh author reruns and source binding checks. Re-executing technical checks does not renew visual/narrative signoff; final review receipts identify those exact canonical files.

For a short source/protection check only:

```powershell
python -B analysis/src/forecast_methods/quant_thesis_validation_v1/verify_evidence.py --out data/processed/forecast_methods/quant_thesis_validation_v1/verification_NEW
```

Inputs: L3 `8821961853e4068febbfe2712f9a4e1036c9e629`, L4 `29b2d9ae2d66da5f1f5086ba7dc36e4c2d96ab70`, original QVS note snapshots in evidence_v3, source audit availability results_v2, and the three hash-frozen protocol documents. L3 has108 bundle files and1187 research/scenario rows, not1187 independent observations. No frozen panel or shared registry/model/scorer is changed. The protocol's actual off-guide historical dates are incompatible with shared harness formats1.0/1.1; full local ledgers preserve diagnostics without inventing an admissible registration date.

Canonical code: source_audit_v1/run.py; uncertainty_audit_v1/run_v2.py; prospective_v1/run.py; expectations_bridge_v2.py; economics_v1/run.py plus publish_v2.py; accounting_review_v1/run_v2.py; independent_reproduction_v1/run_v2.py; adversarial_review_v1/run.py; final_contract_review_v1/run.py; write_claims_v2.py; forecast_figure_v3.py; expectations_figure.py; render_summary.py. Read each package's README and additive README_FINAL_v2 where present. Earlier versioned scripts/results record failed engineering or publication attempts and are not the final interface. Initial freeze_evidence scripts are historical bootstrap records, not routine rebuild commands.

Canonical results: source_audit_v1/results_v3; uncertainty_audit_v1/results_v2; prospective_v1/results_v1; expectations_v2; economics_v1/results_v2; claims_v2; figures_forecast_v3; figures_v1. Canonical one-page summary is docs/revenue-forecast-strategy/quant_thesis_validation_v1/delivery_v3/ABNB_decision_summary.pdf with DECISION_SUMMARY_v3.json. The final presentation-defense version, acceptance gates, reviewer identities, exact hashes and permitted L4 use are listed in the handoff.

Interpretation: W1/W2 accepted evidence uses14/10 targets; the new all-three matched test uses12/10, with2023Q1/Q3 abstentions. W2 is nested. All scenarios are conditional with no fitted probability. Candidate system statistics are four seasonal lambda coefficients plus one policy statistic; inherited K0 selection remains part of the rule. No free weight is fitted or adopted. A passing rebuild is an engineering result, not a passing forecast or investment-evidence gate.
