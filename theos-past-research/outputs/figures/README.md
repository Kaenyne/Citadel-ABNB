# Figure assets

The final report's presentation-ready figure PDFs are retained with their
editable Overleaf source bundle at
`../reports/abnb_macro_to_equity_ic_brief_overleaf/`. Keeping the figures beside
`main.tex` and `metrics.tex` avoids duplicate binaries while preserving the
single source bundle needed to rebuild or review the report.
