# H-005 and H-006 post-freeze replay summary

1. **ABNB Edge-Data Research Orchestrator** — owns orchestration, user communication, and the final decision.
2. **abnb_alt_data** — permanent `gpt-5.6-sol` lead; owns governance, canonical registries, point-in-time standards, and the frozen hypotheses.
3. **physical_world_activity_edge** — physical sensor lane.
4. **supply_scarcity_web_edge** — owns this lane-local H-005/H-006 replay scaffold.

## Result

The replay contains exactly 46 event rows: 23 for frozen H-005 (`ORANGE_FL_TDT_RELEASES`) and 23 for frozen H-006 (`NYC_OSE_STR_SNAPSHOTS`). Both source permission gates are denied, no lawful source observation exists, and no source request was made. Consequently, there are zero eligible source-event observations and no hit, miss, neutral, directional, or predictive classification for either hypothesis.

H-005 has 23 `not_testable` events. H-006 has 11 pre–September 2023 structural `excluded` events and 12 post-regime `not_testable` events. Its historical minimum of eight eligible post-regime events and four consecutive snapshot transitions is therefore unmet. H-005's minimum of 12 eligible numeric events is also unmet.

The governance disposition for both H-005/SCW-006 and H-006/SCW-005 is `INCONCLUSIVE`, not `CONTROL_ONLY`, because each has zero eligible observations and fails its frozen minimum-evidence rule. These are lane-local source decisions and do not imply that all ten supply/scarcity candidates were included in the final combined 15-source approval slate.

## Baselines and compact comparator

The target panel yields 16 numeric/comparable seasonal-naive baselines and 19 numeric/comparable prior-quarter guidance baselines per hypothesis. These baseline values and target changes are descriptive target context only; because the source feature is missing, every supply-signal replay classification remains `not_testable`.

The existing Phase-A H-001 implication/classification is joined without modification for the 16 events where its replay is target/baseline-comparable. It is a control comparator only and is not substituted for either missing supply signal.

## Point-in-time and leakage treatment

Every original cutoff is preserved. Source observation date, reference period, initial publication timestamp, revision/vintage, and feature value remain blank. Current archive indexes, later replacement PDFs, present-day registration snapshots, guidance text, transcripts, and post-cutoff information were not used as source features. No regression, threshold search, fitted combination, or post-hoc feature selection was performed.
