# Prospective collection opportunities — supply/scarcity lane

No collector was started. Every source remains behind an unresolved or failed permission gate.

## Moonshot 1 — SCW-009 federal reservation scarcity panel

If Recreation.gov provides affirmative permission and a documented public availability interface, freeze a panel of major park-gateway facilities and collect daily facility-date availability at fixed lead times (7, 14, 30, 60, 90 days). The primary prospective metric would be `sold_out_share` for future weekend facility-dates. Record rule/release-window changes and never infer history from today's calendar. The main failure mode is that capacity blocks and release rules masquerade as demand.

## Moonshot 2 — SCW-008 San Diego STRO expiry/snapshot panel

If the exact CSV path clears robots, ingest only `tier`, `community_planning_area`, and `date_expiration`, aggregate immediately, and discard the row-level payload. Daily snapshots could measure active-licence net change and expiry concentration prospectively. The main failure mode is administrative renewal timing rather than economically active supply.
