# Proposed quant handoff — not yet produced

Owner: `abnb_alt_data`

Modeling must not begin from Phase A. After approved credential smoke tests and a second unchanged cohort replay, the lead will create `research/quant_handoffs/20260903T053309Z_abnb_readiness/` with the twelve files specified in the agent contract: targets, long features, model matrix, folds, cutoff audit, source provenance, dictionary, reconciled pre-model replay, model spec, summary, manifest, and checksums.

The quant may eventually test H-001 only after expanding-window folds, seasonal and prior-quarter baselines, missing-data rules, and an embargo are frozen. The quant may not use H-002 strict features until exact publication availability is established; the 16:00 UTC field is sensitivity-only. H-003 may not enter a model until each relevant historical CPI release timestamp is proven. No transcript language, guidance statement, later filing, current snapshot, or future vintage may be an input.

Minimum next falsification test: determine whether H-001 improves MAE/RMSE over seasonal and prior-quarter baselines across at least eight walk-forward folds with stable direction under lag sensitivity. The 9/16 descriptive hit count alone is insufficient.

