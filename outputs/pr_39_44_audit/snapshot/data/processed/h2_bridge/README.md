# Original exploratory H1/H2 bridge — interpretation notice

These September 10, 2026 outputs preserve the original Claude-assisted bridge for
reproducibility and context. They are not a validated RNPL forecast or a change to
the live model. Start with [the RNPL handoff](../../../docs/RNPL_HANDOFF.md).

Known interpretation issues: the approximate Q1 three-point nights contribution
was a three-product bundle, not RNPL alone; the RNPL lap overlays are assumed,
not measured from cohort cancellation data; 10–12 is a researcher mapping of
qualitative guidance, not a literal company range. Fixed GBV-to-revenue lags can
also change with booking/payment timing. Broader revenue/event assertions in
the original note were outside the RNPL audit's scope.

Keep these outputs as the original scenario record. Revisit the methods with
better data rather than reading the later caveats as proof they cannot work.
Rebuild with `python analysis/src/h1_to_h2_bridge.py data/raw/fred`.

